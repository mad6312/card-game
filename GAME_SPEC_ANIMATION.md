# カード対戦ゲーム 演出・アニメーション公式仕様書 (ANIMATION & AUDIO)
※本ファイルは全カードのカットイン演出システム、画面配置、各種アクションのビジュアル挙動、連撃ループ、およびBGM・効果音（SE）連携仕様を規定する公式仕様書です。
※カード個別効果は「GAME_SPEC_CARDS_WEAPONS.md」「GAME_SPEC_CARDS_SPECIALS.md」、基本システムは「GAME_SPEC_SYSTEM.md」を参照してください。

---

## 1. 演出システム概要・レイヤー構造

### ■ 概要
攻撃カード発動時、バフ解除時、特殊カード発動時において、画面全体を暗転させた「全画面カットインオーバーレイ」が起動し、迫力あるバトル演出を全プレイヤーへ同期再生する。手札からの自動セット・自動防御が成立した際も、通常通り対象の手前にセットされた防御カードが出現し、「防御！」エフェクトを完全同期再生する。

### ■ レイヤー重なり順（z-index規約）
| レイヤー | z-index | 役割 |
| :--- | :--- | :--- |
| **通常盤面UI** | 1〜100 | 麻雀卓風4人レイアウト、手札、防御カード、ログエリア |
| **各種操作モーダル** | 1,000〜1,100 | 攻撃対象選択、ボーナス得点選択、時限爆弾譲渡、ユーザー設定 |
| **常時表示スコアボード** | 1,500 | 画面左下固定、モーダル操作中も常時点差・順位を表示 |
| **デバッグパネル** | 2,000 | 画面左上固定フローティングUI |
| **全体アナウンスカットイン** | 9,999 | 時限爆弾出現、地震、大災害などの静止画バナーカットイン |
| **画面内自動補正ツールチップ** | 10,000 | カード詳細・バフ説明ツールチップ（画面外見切れ自動反転） |
| **全画面攻撃カットインオーバーレイ** | **99,999** | 最前面。暗転集中線、攻撃者・対象者ユニット、弾道・爆発演出 |

---

## 2. 画面構成と配置パターン（ステージレイアウト）

ステージサイズ: 幅 90% (最大 1,000px) × 高さ 380px。暗転集中線とスピードラインが高速で流れる専用バトルフィールド。

### ① 通常攻撃・グレネード・ダークマター
* **攻撃側ユニット**: 画面左側（`left: 60px`）に固定配置。
  * `[アバター (72x72)]` + `[プレイヤー名]` + `[カードイラスト (95x138)]`
* **対象側ユニット群**: 画面右側（`right: 60px`）に攻撃順・点差順で横並び整列。
  * 各ユニット: `[判定テキストバッジ]` + `[アバター (68x68)]` + `[プレイヤー名]` + `[防御カードポップアップ]` + `[無敵オーラ]`

### ② ダイヤの剣（天空刺突）
* **攻撃側ユニット**: 完全非表示（姿を現さない）。
* **対象側ユニット群**: 画面中央（`left: 50%; transform: translate(-50%, -50%);`）に対称整列。本ターゲットが中心に配置。

### ③ 煙幕（スモーククラウド）
* **使用側ユニット**: 完全非表示（姿を現さない）。
* **対象側ユニット群**: 人数に応じて画面中央に対称配置（1名: 中央 / 2名: 左右等間隔 / 3名: 3等分整列）。静止状態で煙幕に包まれる。

### ④ 無敵アーマー／ステロイド解除時（黄金ショックウェーブ解放）
* **解除者（自分）**: 画面の中央完全固定（`left: 50%; top: 50%;`）。
* **ペナルティ対象者**: 画面中央の自分を挟んで対称に整列：
  * **対象1名**: `[自分 (中央)] ── [対象 (右)]`
  * **対象2名**: `[対象A (左)] ── [自分 (中央)] ── [対象B (右)]`
  * **対象3名**: `[対象A (左外)] ── [対象B (左内)] ── [自分 (中央)] ── [対象C (右)]`
* ※「選択不可状態」のプレイヤーはカットイン演出自体から除外（非表示化）。

---

## 3. アクション挙動と判定別演出仕様

### ■ 攻撃側アクション
1. **突進（木の盾・木の盾セット・青銅の盾・青銅の盾セット・木の剣・木の剣セット）**:
   * 攻撃ユニットが左から右の対象の手前（`targetX - 150px`）に向かって急加速で突進。
2. **射撃・マズルフラッシュ・リコイル（ショットガン専用）**:
   * 攻撃ユニットは左側に留まり、発射時に `recoil-action`（左へ22pxノックバック後復帰）。
   * 銃口から `cutin-muzzle-flash`（白熱フラッシュ）が炸裂し、超高速の光線弾トレーサー（`cutin-bullet-tracer`）が発射される。
3. **手榴弾投擲（グレネード専用）**:
   * 手元から手榴弾（`cutin-grenade-bomb`）が回転しながら山なりの放物線を描いて対象群の中心へ飛翔。
4. **闇のオーブ投擲（ダークマター専用）**:
   * 手元から紫黒のダークマターオーブが720度回転しながら対象群の中心へ飛翔。
5. **黄金ショックウェーブ解放（無敵アーマー／ステロイド解除専用）**:
   * 中央の解除者の足元・周囲から巨大な金色のショックウェーブ波動（`cutin-buff-expire-blast`）が全画面へ炸裂。
6. **煙玉落下＆煙幕大拡散（煙幕専用）**:
   * 画面上端（天空）から煙玉が回転急降下して中央に着弾。直後に巨大な黒煙（`cutin-smoke-cloud`）がモクモクと急拡大して対象群を包み込む。
7. **天空刺突＆クリスタル大爆発（ダイヤの剣専用）**:
   * 画面上端から巨大なダイヤの剣が光の粒子を纏って急降下し、本ターゲットの足元へ深く突き刺さる。突き刺さった瞬間にクリスタル大爆発が炸裂。

### ■ 判定別リアクション演出
1. **回避（ミス）**:
   * 対象ユニットが上方向へサッと飛び退いて半透明化（`dodge-action`）。
   * 青文字で「**回避！**」バッジが出現。
2. **命中（通常ヒット）**:
   * 画面全体が白く激しくフラッシュ（`cutin-flash-overlay`）。
   * 赤文字で「**命中！**」バッジが出現。
   * 対象ユニットが回転しながら画面右外へ吹き飛ぶ（`blown-away-action`）。
3. **防御（通常防御・手札自動セット防御）**:
   * 対象の手前にセット中の防御カードがポップアップ出現（`cutin-def-shield-card`）。
   * 緑文字で「**防御！**」バッジが出現。攻撃側ユニットが火花とともにノックバック（`attacker-knockback`）。
4. **貫通（ショットガン・グレネード）**:
   * 対象の手前に防御カードが出現するが、直撃を受けて紫文字で「**貫通！**」バッジが出現。
   * 防御カードが粉砕・吹き飛び（`cutin-def-shield-card.broken`）、対象ユニットも同時に画面外へ吹き飛ぶ。
5. **無敵 / ステロイド（耐え判定）**:
   * 対象の周囲に金色のバリアオーラ（`cutin-invincible-aura`）が出現。
   * 橙文字で「**無敵！**」「**ステロイド！**」バッジが出現。対象は微動だにせず攻撃を受け流す。
6. **扇状同時吹き飛び（グレネード・ダイヤの剣・ダークマター・解除時）**:
   * 直撃中心から、被災者たちが配置位置に応じて一斉・同時に放射状（左斜め上 `blow-left-up` / 真上 `blow-straight-up` / 右斜め上 `blow-right-up`）へ吹き飛ぶ。
   * 自身が巻き込まれた攻撃者自身は左外（`blow-self-left`）へ吹き飛ぶ。

---

## 4. 複数回攻撃（連撃ループ）の制御仕様

* 対象カード: 「木の盾セット」「青銅の盾セット」「木の剣セット」
* 指定攻撃回数（1〜3回）に応じたラウンド制連撃ループを実行。
* 命中して吹き飛んだプレイヤーは盤面外に離脱した状態を維持し、次撃は残存している対象へ初期位置から再度突進。
* **動的ターゲット移行（青銅の盾セット パターンA）**: 1撃目で相手が吹き飛んだ場合、2撃目は次に近い相手へ自動でターゲット移行して突進。
* **単体連続突進（木の剣セット パターンA）**: ミス・防御時は同じ相手へ連続突進し、命中した瞬間に吹き飛ばして連撃完了。
* **中断条件**: 回数終了、対象消失、または無敵・ステロイド状態で無効化された場合に即座に連撃ループを終了。

---

## 5. 完全同期キューイングシステム

* **サーバー同期**: サーバー側で攻撃・カウンター・ダメージの全判定結果シーケンスを確定後に演出イベント（`playAttackCutin`）を発火。
* **クライアント側キュー**:
  * 多段カットイン（例: 攻撃被弾直前の手札ダークマター自動発動、バフ解除時の連鎖発動など）は、先行カットインおよびLP変動の完了を待ってから順次再生。
  * カットイン再生中はLPドラムロールおよびポップアップを保留し、演出終了と同時に数値が吸い込まれる美しい同期を実現。

---

## 6. オーディオ（BGM・効果音 SE）連携仕様規約

※今後のBGM・SEマネージャー実装時に本定義に沿ってサウンドを完全連動させます。

### ■ BGM仕様
* **メインBGM**: 対戦中ループ再生（音量調整可能）。
* **クライマックスBGM**: 第8巡目以降または1位と僅差の終盤にテンポアップBGMへシームレス切り替え。

### ■ 効果音（SE）再生トリガー一覧
| イベント | 再生タイミング | 想定サウンド |
| :--- | :--- | :--- |
| **突進開始** | 突進ユニットが加速した瞬間 | 高速ダッシュ・風切り音 (`se_dash`) |
| **ショットガン発射** | マズルフラッシュと同時に発射 | 重火器銃声・リコイル音 (`se_shotgun`) |
| **グレネード投擲** | 手榴弾が回転飛翔する間 | ピン抜き・投擲回転音 (`se_throw`) |
| **爆発・着弾** | グレネード・クリスタル・闇オーブ炸裂時 | 迫力の大爆発音 (`se_explosion`) |
| **命中ヒット** | 白フラッシュと吹き飛び発生時 | 強烈な打撃・被弾音 (`se_hit`) |
| **通常防御** | 防御カードポップアップ時 | 金属盾の防御・火花音 (`se_block`) |
| **防御貫通** | 貫通バッジ出現・防御カード粉砕時 | 盾粉砕・弾丸貫通音 (`se_pierce`) |
| **無敵耐え** | 金色バリアオーラ出現時 | 神聖なバリア共鳴音 (`se_barrier`) |
| **回避** | キャラが上へ飛び退いた時 | シュッという身かわし音 (`se_dodge`) |
| **天空刺突** | ダイヤの剣が地面へ突き刺さる瞬間 | 金属重剣の突き刺さり音 (`se_sword_impact`) |
| **煙幕拡散** | 煙玉着弾から煙が広がる瞬間 | シューというガス噴出音 (`se_smoke`) |
| **LPカウント変動** | ドラムロール数値変動時 | デジタルLP高速カウント音 (`se_lp_roll`) |
| **LPカウント確定** | 数値確定フラッシュ時 | 決定・パルス音 (`se_lp_fix`) |