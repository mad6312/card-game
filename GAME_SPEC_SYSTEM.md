# カード対戦ゲーム システム・UI公式仕様書 (SYSTEM & UI)
※本ファイルはゲームの基本進行・状態変化・カウンター防御・UI・文言規約を規定する公式仕様書です。
※武具カード個別効果は「GAME_SPEC_CARDS_WEAPONS.md」、特殊・魔法カードは「GAME_SPEC_CARDS_SPECIALS.md」、演出システムは「GAME_SPEC_ANIMATION.md」を参照してください。

---

## 1. 基本ルール・ゲーム進行

### ■ プレイヤー・勝敗
* **プレイ人数**: 4人 (P1, P2, P3, P4)
* **初期持ち点**: 25,000点 / 初期手札: 0枚
* **ゲーム終了条件**: 
  * 第10巡目（第10ラウンド）において、全4プレイヤーの手番行動が完了した瞬間にゲームが進行停止する（※11巡目へは突入しない）。
  * 終了と同時に最前面に「リザルト画面」がポップアップ出現し、最も最終持ち点が高いプレイヤーが勝者となる。

### ■ 任意参加エントリー制（ロビー待機・キャンセル・事前設定・切断安全装置）
* **アクセス時の状態**: URLにアクセスした時点では自動参加せず、全プレイヤー「未エントリー（観戦/ロビー待機状態）」となる。
* **エントリー方法**:
  * 画面中央の「🎮 参加する」ボタンを押下することで、初めてゲーム参加状態となり「参加中（X/4人）」としてカウントされる。
* **参加キャンセル（エントリー解除）**:
  * 4人揃ってゲームが開始する前であれば、もう一度ボタン（「✖ 参加キャンセル」）を押下することでいつでもエントリーを解除できる。
  * キャンセル時は参加人数カウントから除外され、他の待機プレイヤーへ即座に最新人数が同期される。
* **プレイヤー番号（P1〜P4）の動的割り当て**:
  * 1〜4番の中で「現在使われていない最小の番号」が自動割り当てられる。
  * 途中で参加キャンセルが発生しても、再エントリー時に空き番号が再利用されるため、重複や歯抜けが一切発生しない。
* **事前プロファイル設定（参加前変更・即時フィードバック）**:
  * 「参加する」ボタンを押下する前であっても、画面右上の「⚙ 設定」ボタンから「プレイヤーネーム」や「アバター」を自由に変更・設定できる。
  * 設定モーダルを開いたまま名前変更（「✓ 変更しました！」表示）やアバターの即時枠線強調切り替えが可能。
  * エントリー時に、設定済みの名前とアバター情報がそのまま盤面に反映される。
* **ゲーム自動開始トリガー**:
  * 定員（4名）が「参加する」を押下した瞬間にロビー画面が閉じ、初期得点ドラフトフェーズ（またはデバッグ時はメインゲーム）へ遷移する。
* **途中切断時（離脱時）の安全装置（進行不能防止・ロビー自動帰還）**:
  1. **開始前（ロビー待機中）の切断**:
     * 自動的にエントリー解除（キャンセル扱い）となり、残りの待機者へ人数更新が即座に同期される。
  2. **対戦中・ドラフト中の切断（安全中断＆参加維持）**:
     * ゲーム進行中（ドラフト中、または第1巡〜第10巡の対戦中）にいずれかのプレイヤーが回線切断（ブラウザを閉じる等）した場合、ゲームが進行不能にフリーズすることを防ぐため、即座に対戦が安全中断される。
     * サーバー側のゲーム状態（`gameState`）を初期化し、全画面カットインや開いているモーダルを強制消去する。
     * 残されたプレイヤー（3名）は**「参加中（3/4人）」のエントリー済み状態を自動維持**したままロビー待機画面へスムーズに帰還する。
     * 画面上に「⚠️ 〇〇が回線切断したため、対戦を中断しロビーへ戻りました。（あなたはエントリー状態のまま待機中です）」という通知が表示される。
     * 切断してしまったプレイヤーが再アクセスして「参加する」を1回押すだけで、一瞬で4名揃い、即座に新しい対戦を開始できる。

### ■ ゲーム開始前：初期得点ドラフトフェーズ（本番仕様）
定員4名のエントリー完了後、メインゲーム開始前に初期持ち点（25,000点）に加減算される得点を決定する心理戦ドラフトを実施する。
* **初期得点プール**: **`[+5,000点, +1,000点, -1,000点, -5,000点]`**
* **同時選択とランダム勝敗抽選ロジック**:
  1. 全プレイヤーが秘密裏に希望する得点ボタンを1つ選択・送信。
  2. **単独選出（1名のみ選択）**:
     * そのプレイヤーが該当得点を無条件で獲得・確定（プールから除外）。
  3. **バッティング競合（2名以上が同一選択）**:
     * システムが自動的に**ランダムで1名の勝者を抽選決定**。
     * **勝者（1名）**: その得点をそのまま獲得・確定（プールから除外）。
     * **敗北者（1名〜複数名）**: 未確定のまま、残存得点プールを用いて再選択を実施。
  4. **【自動割り当ての例外ルール】**:
     * 未確定プレイヤーが残り1名、かつ残存得点が1つの状況になった場合は、再選択を行わずに**自動的にその最後の得点を対象者に割り当てて確定**し、直ちにメインゲームが開始される。

### ■ ターン順の決定ロジック（動的ターン順）
各巡目（ラウンド）において、まだその巡で行動していないプレイヤーの中から以下のように次の手番を決定する。
1. **1巡目かつ全員同点の場合**: P1 → P2 → P3 → P4 の昇順で行動。
2. **通常時（ドラフト後含む）**: 未行動のプレイヤーの中で **最も得点が高いプレイヤー** から順に行動。
   * 最高得点が同点の候補者が複数いる場合、その中からランダムで決定。

### ■ 1巡目の共通制限ルール（システム管理）
1. **攻撃対象制限**: 1巡目は、**自分より後に行動するプレイヤー** を攻撃対象として選択できない（UI上の選択肢からも除外）。
2. **カード効果無効**: 1巡目は、**自分より前に行動するプレイヤーが使用したカードの効果を受けない**（全体効果・特殊効果含む）。

### ■ ターンの流れ
1. **ターン開始時（ボーナス・ドローフェーズ）**:
   * **獲得得点の選択**: モーダル上で **+3,000点 〜 -3,000点（1,000点刻み、初期値: +3,000点）** から獲得したい得点を選択し「獲得する」ボタンを押下して確定。
   * ※デバッグ設定「ボーナススキップ」がONの場合は、ダイアログを表示せず自動的に **「獲得得点: 0点（スコア加減算なし）」** として即座にスキップ処理される。
   * **山札ドロー**:
     * 各種制限ルール（出現巡目・重複出現・重複所持・12Tクールダウン）を優先判定して不可能なカードを除外。
     * 制限をクリアした有効カード群の中から、プレイヤーの現在順位に応じた**「順位別カード排出確率テーブル」**に基づき重み付け抽選を行ってカードを1枚ドロー（※時限爆弾を引いた場合は手札に入らず即時強制発動）。
2. **メインフェーズ**:
   * 手札のカードを1枚クリックして使用、または防御カードへセット。
   * セット中の防御カードを外して攻撃、または破棄。
   * 自身が「時限爆弾状態」の場合、バッジをクリックして他者への受け渡しを試行可能（自ターン継続）。
   * 「ターン終了」ボタンで手番を終了（未挑戦の時限爆弾と対象がある場合は安全装置モーダルが表示）。
3. **手札調整フェーズ（手札オーバー時）**:
   * ターン終了時に手札が2枚以上ある場合、1枚以下になるまで捨てるカードを選択。

---

## 2. 順位別カード排出確率テーブル＆抽選アルゴリズム

### ■ 順位別カード排出確率テーブル（各順位合計 100.0%）
プレイヤーの現在順位（1位〜4位）に応じて、以下の確率テーブルに基づいてカードが抽選される。

| カード名 | 1位 (計100%) | 2位 (計100%) | 3位 (計100%) | 4位 (計100%) |
| :--- | :---: | :---: | :---: | :---: |
| **木の盾** | 32.5% | 5.0% | - | - |
| **木の盾セット** | - | 10.0% | 2.5% | - |
| **青銅の盾** | - | 17.5% | 2.5% | - |
| **青銅の盾セット** | - | 7.5% | 5.0% | - |
| **木の剣** | 37.5% | 2.5% | - | - |
| **木の剣セット** | 10.0% | 5.0% | - | - |
| **ショットガン** | 20.0% | 2.5% | - | - |
| **グレネード** | - | 7.5% | 2.5% | - |
| **ダイヤの剣** | - | 7.5% | 15.0% | 5.0% |
| **地震** | - | 2.5% | 10.0% | 5.0% |
| **時限爆弾** | - | 7.5% | 7.5% | - |
| **お守り小判** | - | 15.0% | 2.5% | - |
| **お守り小判セット** | - | 2.5% | 12.5% | 17.5% |
| **お守り大判** | - | - | 7.5% | 22.5% |
| **大災害** | - | - | 5.0% | 20.0% |
| **無敵アーマー** | - | - | 7.5% | 10.0% |
| **ダークマター** | - | - | - | 17.5% |
| **ステロイド** | - | 5.0% | 10.0% | - |
| **煙幕** | - | 2.5% | 10.0% | 2.5% |

### ■ 重み付け再正規化抽選アルゴリズム
1. **制限カードの除外**: 各種制限（出現巡目・重複出現・重複所持・12Tクールダウン）に抵触したカードを候補から除外。
2. **残存重みの比例配分**:
   * 残った有効カード群の確率値（重み: Weight）の合計 $W_{\text{total}}$ を算出。
   * 各有効カードの元の確率比率を維持したまま、$W_{\text{total}}$ を母数としてランダム抽選を実施。
   * ➔ 除外されたカードの確率分が、残存する有効カードへ**元の比率通りに自動的に綺麗に再配分**される。
3. **同点順位の扱い**:
   * 得点が同点のプレイヤーが複数存在する場合、システム上の順位（例: 25,000点で全員同点なら全員1位）に基づき、同じ順位のテーブルを参照する。
4. **安全フォールバック（候補0件時の安全措置）**:
   * 制限により該当順位テーブルの有効カードが0件になった場合、直近の上位順位テーブル（3位 ➔ 2位 ➔ 1位）へフォールバックして探索し、ゲームがクラッシュすることなく必ず有効なカードが選出される。

---

## 3. 状態変化（バフ・デバフ）と防御・手札自動発動（カウンター）仕様

### ■ 状態変化一覧
| 状態名 | 種類 | 公開範囲 | タイマー減少タイミング | 効果概要 |
| :--- | :--- | :--- | :--- | :--- |
| **無敵（無敵アーマー）** | バフ | **初期非公開**<br>（攻撃防御で全公開） | **誰かのターンが1回終了する毎に -1** (初期値: 4T) | あらゆる攻撃カードの効果を受けない。<br>【獲得時】自身に付与されていた「暗闇状態」を即座に解除。<br>【解除時】+1,000点 ＆ 条件を満たす相手へ50%で手札・防御全破棄 / -3,000点 / 選択不可付与。 |
| **ステロイド** | バフ | **初期非公開**<br>（攻撃防御で全公開） | **誰かのターンが1回終了する毎に -1** (初期値: 4T) | 一部を除く攻撃カードの効果を受けない（大災害を受けると即解除）。<br>【獲得時】自身に付与されていた「暗闇状態」を即座に解除。<br>【解除時】+1,000点 ＆ 条件を満たす相手（ステロイド除く）へ50%で手札・防御全破棄 / -3,000点 / 選択不可付与。 |
| **無敵（ダークマター）** | バフ | **常時公開** | **使用者の次の自ターン開始時に解除** | あらゆる攻撃カードの効果を受けない。<br>【獲得時】自身に付与されていた「暗闇状態」を即座に解除。 |
| **暗闇（煙幕）** | デバフ | **常時公開** | **付与されたプレイヤー自身がターン終了する毎に -1** | 攻撃カード使用時の命中率が半減する。<br>継続ターン: 対象が1位なら2T、2位〜4位なら1T。<br>※自身が無敵またはステロイド状態になった瞬間に即座に解除される。 |
| **選択不可** | 状態 | **常時公開** | **付与されたプレイヤー以外の誰かのターンが1回終了する毎に -1** (初期値: 2) | 攻撃カードの対象として選択できなくなる。 |
| **時限爆弾状態** | 特殊状態 | **常時公開** | **誰かのターンが1回終了する毎に -1** (初期値: 8T) | 自ターン開始毎に+1,000点。±3,000点差の相手に50%で受け渡し可能。無敵・ステロイドで消滅。<br>【カウント0時】爆発して -6,000点 / 手札・防御全破棄 / 選択不可(2T上書き)付与（※手札カウンター発動時は爆発前に消滅・無効化）。 |

### ■ バフバッジの公開範囲・動的切り替え仕様
* **無敵アーマー／ステロイド**:
  * **初期状態**: 使用者本人にのみバッジを表示（他プレイヤーからは非公開）。カード使用ログも他者には伏せられる。
  * **公開条件**: 該当のバフ効果によって攻撃を1度でも防いだ（無効化した）瞬間、または手札からの自動発動（カウンター防御）が発動した瞬間。
  * **変化後**: 攻撃を防いだ時点から、全プレイヤーに対してステータスバッジ（残ターン数含む）が可視化される（スコアボード上でも同様に公開）。
  * **解除時**: 効果終了とともに非表示化・リセットされる。
* **時限爆弾状態**:
  * 常に全プレイヤーに公開され、残りターン数がバッジとして表示される。

---

### ■ 防御カードの手札自動セット・自動防御仕様
防御カードとしてセット可能なすべてのカード（全7種）は、手札に所持している状態で無効化可能な攻撃を受けた際、被弾確定直前に自動的に防御スロットへセットされ、攻撃を無効化する。

#### 1. 対象カード（全7種）
* **格上防御可能カード（4種）**: 「木の盾」「木の盾セット」「青銅の盾」「青銅の盾セット」
* **格上防御不可カード（3種）**: 「木の剣」「木の剣セット」「グレネード」

#### 2. 発動条件（トリガー）
* 相手からの攻撃が自分に命中し、かつその攻撃が防御カードによって無効化可能な攻撃（通常攻撃）である場合。
* ※攻撃が回避（ミス）となった場合は発動せず、手札に対象カードが温存される。
* ※防御カード貫通攻撃（「ショットガン」「グレネード（攻撃）」）や、防御不可の全体攻撃（「ダイヤの剣」「地震」「大災害」「煙幕」）に対しては防御カードは自動セットされず、無敵系カードまたはお守りが発動する。

#### 3. 自動セット時の優先順位
手札に複数の防御可能カードを所持している場合、以下の優先度に従って1枚のみ自動セットされる：
* **【格上からの攻撃時】（格上防御可能カードのみ対象）**:
  1. **木の盾**（使い切りカードを優先消費）
  2. **青銅の盾**
  3. **木の盾セット**（耐久回数3回）
  4. **青銅の盾セット**
* **【同点・格下からの攻撃時】（全7種が防御有効）**:
  1. **木の剣**（防御性能が限定的なカードを優先消費）
  2. **木の剣セット**
  3. **木の盾**
  4. **青銅の盾**
  5. **木の盾セット**
  6. **青銅の盾セット**
  7. **グレネード**（※カウンター反撃を温存するため最後尾優先）

#### 4. 既存セットカードの「上位カード上書き破棄」ルール
* 既に防御カードがセットされている場合、原則としてセット済みのカードを優先消費して攻撃を防ぐ。
* **【例外（上書き破棄）】**: セットされているカードが「格上攻撃を防げないカード（木の剣/木の剣セット/グレネード）」であり、自分より得点が高いプレイヤー（格上）から攻撃を受けた場合：
  * 手札に「格上防御可能カード（木の盾/木の盾セット/青銅の盾/青銅の盾セット）」が存在すれば、**セットされていたカードを即座に破棄（墓地送り）し、手札の格上対応カードを自動セットして攻撃を無効化**する。
  * 手札に格上対応カードが存在しない場合は、既存カードは格上攻撃のため貫通され、後続の無敵系判定またはダメージ処理へ移行する。

#### 5. 発動後の状態・公開ルール
* 手札から自動セットされて攻撃を防いだカードは、相手へ公開（`revealed = true`）される。
* 残り耐久回数が 1 以上のカード（セットカード等）は、防御スロットにそのまま残り、次回以降の防御として機能する。使用回数が 0 になったカードは通常通り破棄される。

---

### ■ 手札からの緊急自動発動（カウンター防御：無敵系）仕様
「無敵アーマー」「ステロイド」「ダークマター」を手札に所持している場合、攻撃やペナルティを受けた際に手札から自動消費されて割り込み防御が発動する。

#### 1. 発動条件（トリガー）
* 対象カードを手札に所持している状態で、「大災害」「煙幕」「ターン開始ボーナス減点」**以外**の攻撃・ペナルティ（時限爆弾の爆発含む）により、自身にダメージまたはペナルティ（手札破棄等）が発生することが確定した瞬間。
* **※手札に防御カードがある場合は防御カードが優先して自動セット・発動し、防御カードで防げない場合のみ無敵系カードが自動発動する。**

#### 2. 発動時の処理順序（割り込みライフサイクル）
攻撃が命中・ペナルティ発生が確定した直前、以下の順で処理が割り込む：
1. **ステップⅠ（セット済み防御カードの全破棄）**: 自分にセットされている防御カードをすべて破棄する。
2. **ステップⅡ（カードの自動使用・状態適用）**: 対象カードを手札から1枚自動消費し、自身に「無敵状態」「ステロイド状態」等を適用する（※即座に公開バッジ化、時限爆弾および暗闇状態は即座に消滅・解除）。
   * ※「ダークマター」が自動発動した場合、手動時と同様に **「+5,000点獲得」および「同点・逆転相手への50%ペナルティ判定」** も実行される。
3. **ステップⅢ（元の攻撃・ペナルティの処理）**: 適用された状態により、元の攻撃・ペナルティ（ダメージ、手札破棄、選択不可）を無効化する。

#### 3. 自動発動カードの優先順位（判定順）
手札に複数枚の対象カードを所持している場合、受ける攻撃の種類に応じて以下の順で1枚のみ消費される：
* **【ケースA】「ステロイド状態」でも無効化できるダメージ・ペナルティの場合**（通常の攻撃カード、ダイヤの剣、地震、グレネード、時限爆弾爆発など）:
  1. **ステロイド**（最優先）
  2. **無敵アーマー**
  3. **ダークマター**
* **【ケースB】「ステロイド状態」では無効化できないダメージ・ペナルティの場合**（無敵アーマー解除時・ダークマター使用時のペナルティなど）:
  1. **無敵アーマー**（最優先：完全無効化）
  2. **ダークマター**（2番手：完全無効化＆+5,000点）
  3. **ステロイド**（3番手：無駄消費となるが自動使用されステロイド状態4Tを付与。直後にペナルティを受けて手札・防御は破棄される）

---

### ■ 防御効果の処理優先順位（総合判定順序）
攻撃が命中した際、各種防御・バフ・カウンターが適用される厳密な優先順位：
1. **第1優先（セット中防御カード）**:
   * セット中の防御カードで使用回数を1消費して無効化。
   * ※格上攻撃時の「木の剣/木の剣セット/グレネード」の場合、手札に格上防御可能カードがあれば**既存カードを破棄して手札から自動セット＆防御**を実行。
2. **第2優先（既存バフ状態）**:
   * 防御カードがない、または貫通された場合、既に付与されている「無敵」「ステロイド」状態による無効化判定を行う（※既に無敵・ステロイド状態の場合、手札の防御カード・無敵カードは無駄消費されず温存される）。
3. **第3優先（手札からの防御カード自動セット・自動防御）**:
   * 自身が無敵等でなく、受ける攻撃が通常攻撃の場合、手札の防御可能カードが優先度順（格上防御時 / 同点・格下防御時）に従って自動スロットセットされ、攻撃を無効化する。
4. **第4優先（ダイヤの剣専用：お守り自動身代わり）**:
   * ダイヤの剣を受けた際、自身が無敵/ステロイド状態でなければ、手札の「お守り（大判 > 小判セット > 小判）」を自動消費して回避（※セット中防御カードは全破棄）。
5. **第5優先（手札カウンター自動発動：無敵系）**:
   * 被害が確定する直前に、手札の「ステロイド」「無敵アーマー」「ダークマター」が優先順（ケースA/B）に従って自動発動し、セット中防御カードを破棄した上で状態を適用・無効化（※暗闇状態は即座に解除）。
6. **第6優先（ダメージ・効果適用）**:
   * いずれの防御も行われなかった場合、得点ダメージおよび手札破棄、選択不可などのデバフを適用。

### ■ ターン終了時のタイマー減算順序（厳密仕様）
ターン終了時は、以下の**2段階処理**でタイマーを更新する。
1. **ステップ1（既存タイマーの減算）**:
   * 現在手番を終えたプレイヤー自身の「暗闇」タイマーを -1。
   * 手番を終えたプレイヤー**以外**の既存の「選択不可」タイマーを -1。
   * **使用後出現制限（12Tクールダウンタイマー）の減算**: ダイヤの剣、地震、大災害、煙幕の各カードのタイマーを -1。
2. **ステップ2（バフ解除・ペナルティ判定・時限爆弾処理）**:
   * 「無敵アーマー」「ステロイド」「時限爆弾」のタイマーを -1。
   * 無敵アーマー／ステロイドが 0 になった場合、解除時効果（+1,000点＆相手へのペナルティ判定）を実行（※相手の手札カウンター割り込み可能）。
   * 時限爆弾が 0 になった場合、手札カウンター判定を経て、発動がなければ爆発ペナルティ（-6,000点、手札防御全破棄、選択不可2T上書き）を実行。
   * ※解除ペナルティで付与された「選択不可（2）」は、ステップ1の減算完了後に付与されるため、**付与されたターン終了時に即座に減算されることはなく、確実に2回分の手番終了まで持続する**。

---

## 4. UI・表示仕様

### ■ 常時表示スコアボードUI（4カラムGrid・状態バッジ表示・アコーディオン折りたたみ・可変幅fit-content・盤面接触防止・モーダル最前面）
* **概要**: 自分と他プレイヤーとのリアルタイムな得点差、順位、および全プレイヤーの現在の状態変化（バフ・デバフ）を一目で常時確認できる画面左下配置のコンパクトスコアボード。
* **設置位置＆重なり順**:
  * **設置座標**: `position: fixed; bottom: 20px; left: 20px;`（画面左下）
  * **重なり順**: **`z-index: 1500`**（モーダルUI[1000〜1100]より前面、カットイン演出[99999]の背面）。
  * ➔ モーダルで攻撃対象や獲得得点を選択中であっても常時点差・順位・相手状態を確認可能。
* **アコーディオン折りたたみ機能（ネイティブ開閉制御）**:
  * **初期状態**: 画面表示時は**「開いている（展開: `open`）状態」**。
  * **トグル操作**: ヘッダー（「📊 スコアボード」）をクリックすることで、いつでも折りたたみ（閉じる）／展開をスムーズに切り替え可能。
  * **開閉状態の永続維持**: 得点変動やドラムロール更新が実行されても、ユーザーが手動で折りたたんだ状態（閉じた状態）は勝手に再展開されず、確実に維持される。
  * **折りたたみ時のスタイル**: 閉じた際は「📊 スコアボード」というコンパクトなタブ形状になり、盤面の視界を最大限に確保できる。
* **4カラムGridレイアウト（縦列統一整列）**:
  * **カラム1（プレイヤー情報）**: `[順位] + [アバター画像(22x22)] + [プレイヤー名]`（平仮名10文字まで省略されずに全文表示）。
  * **カラム2（状態ステータスバッジ群）**: `[ミニバッジ表示]`（左揃え・gap: 4px。選択不可タグ、無敵・ステロイド・煙幕・時限爆弾の極小ミニアイコン[16x24]＋右上赤丸残ターン数）。
    * ※他プレイヤーの「無敵アーマー」「ステロイド」は、初期非公開時はスコアボード上でも非表示。攻撃を防いで全公開された後に可視化。
    * ※バッジが付与されていない行は空白となり、右側の得点・点差カラムの縦列整列が美しく統一される。
    * ※バッジホバー時には盤面UIと同様に画面内自動補正ツールチップが出現。
  * **カラム3（現在の得点）**: `[〇〇点]`（右揃えで縦列統一）。
  * **カラム4（自分との得点差）**: `[タグ表示]`（右揃えで縦列統一）。
* **得点差表記ルール＆カラーリング**:
  * 自分より上位：<span style="color:#2ecc71; font-weight:bold;">(+〇〇点)</span>（緑色）
  * 自分自身：<span style="color:#38bdf8; font-weight:bold;">(自分)</span>（シアン）
  * 自分より下位：<span style="color:#e74c3c; font-weight:bold;">(-〇〇点)</span>（赤色）
  * 自分と同点：<span style="color:#f1c40f; font-weight:bold;">(±0点)</span>（金色）
* **可変幅（伸縮）＆盤面接触防止ルール**:
  * プレイヤー名やバッジ数に応じて横幅が自動伸縮（展開時: `min-width: 330px;` / 折りたたみ時: `min-width: auto;`）。
  * 画面中央（940px）に配置されたメイン盤面UIに接触・重なる直前で伸縮をストップ（`max-width: min(480px, calc(50vw - 490px))`）。
* **更新同期タイミング**: 得点変動時、ドラムロール演出が完了して数値が確定した瞬間に最新スコア状況でリアルタイムソート＆差分更新を実行。

### ■ リザルト画面UI（2カラムGrid・1位ゴールドスポットライト演出・勝敗遷移）
* **概要**: 全10巡終了時に最前面にポップアップ出現し、最終結果をドラマチックに演出するリザルト画面。
* **重なり順**: **`z-index: 2500`**（スコアボードや各種操作モーダルよりも前面に表示）。
* **画面中央揃え2カラムGridレイアウト（縦列統一整列）**:
  * **カラム1（プレイヤー情報）**: `[順位]` ＋ `[アバター画像 (26x26)]` ＋ `[プレイヤー名]`（ひらがな最大10文字を省略せず全文表示） ＋ `[🏆 WINNERバッジ (1位のみ)]`
  * **カラム2（最終得点）**: `[〇〇点]`（右揃えで縦列統一）
* **1位（勝者）のゴールドスポットライト演出仕様**:
  1. **背景スポットライト光線（光錐エフェクト）**:
     * リザルト画面背面上部から1位ユニットへ向かって金色の半透明円錐ライトが差し込み、ゆっくりと明滅パルスを繰り返す専用エフェクト（CSSアニメーション）。
  2. **1位専用ゴールドフレーム＆パルスグロー**:
     * 2位〜4位の行はダークトーン（`opacity: 0.72`）に抑えられ、**1位の行だけが前面にポップアップ浮遊（`scale(1.03); z-index: 5;`）**。
     * 黄金のグラデーション背景、`border: 2px solid #f1c40f`、およびネオングロー（`box-shadow: 0 0 25px rgba(241,196,15,0.7)`）が付与される。
  3. **👑 1位王冠アイコン ＆ 🏆 WINNERバッジ**:
     * 1位の順位テキストに上下に跳ねる王冠アイコン（👑）が付き、名前の右隣にゴールドグラデーションの「🏆 WINNER」バッジが表示される。
  4. **同点1位（タイ）対応**:
     * 1位が同点で複数名いる場合、該当者全員にゴールドフレームと王冠が付与され、全員が勝者として称えられる。
* **ボタン操作と状態遷移**:
  * 順位一覧の下部に `[ 🔄 再戦 ]` と `[ 🚪 退出する ]` ボタンを配置。
  * **`[ 🔄 再戦 ]` ボタン**:
    * 押下時、サーバーへエントリー要求を送信し、リザルト画面を閉じてロビー待機状態（`参加中... 他のプレイヤーを待っています`）へ遷移。
    * 持ち点（25,000点）や手札・防御カード・タイマー等は初期値へ完全リセットされ、4人揃い次第ドラフトフェーズ（またはゲーム）が自動開始される。
  * **`[ 🚪 退出する ]` ボタン**:
    * 押下時、参加リストから除外され、リザルト画面を閉じてロビー待機状態（`[ 🎮 参加する ]` ボタン表示の未エントリー状態）へ遷移。名前やアバターの再設定が可能となる。

### ■ 数値の3桁カンマ区切り（カンマセパレート）表示の統一
* 画面上の持ち点、変動スコア、ツールチップ説明文、各種モーダル内の選択肢およびシステムメッセージに表示されるすべての数値は、3桁カンマ区切り（例: `25,000点`、`+3,000点`、`-5,000点`）で統一表示される。

### ■ ライフポイント（LP）風得点変動アニメーション演出
* **ステップⅠ（変動値ポップアップ）**: 得点変動時、対象プレイヤーの「持ち点: 〇〇点」テキストの**すぐ右隣（fit-content配置）**にプラス（緑色 `+3,000`）／マイナス（赤色 `-3,000`）の数値がポップアップ出現（表示時間: 1.15秒）。
* **ステップⅡ（吸い込み合流）**: ポップアップした変動値が持ち点数値へ向かってスッと吸い込まれるように移動・フェードアウト。
* **ステップⅢ（LPドラムロール変動）**: 吸い込みと同期して持ち点数値が高速カウントアップ／ダウン（750ms）し、完了時に確定パルス（フラッシュ）が発生。カットイン終了後に確実に発火するキュー待機構造を適用。

### ■ 画面右上「設定メニュー（⚙ボタン）」
* 画面右上の⚙ボタンから、プレイヤーごとの独立したユーザー設定モーダルを開く。
* **プレイヤーネーム変更機能**: 任意の表示名（最大10文字）を入力・変更可能。モーダルを開いたまま名前を変更でき、「✓ 変更しました！」という完了メッセージが表示される。盤面・ダイアログ・ログへ即座に新名称が同期反映される。
* **プレイヤーアバター設定機能**: 
  * 盤面UI上に正方形枠（1:1）でアバター画像を表示。
  * **初期割り当て**: ゲーム開始時、全プレイヤーに共通の「デフォルトアバター（avatar_default）」が初期設定される。
  * **選択・枠線強調切り替え**: モーダルを開いた時点で現在のアバターが強調表示され、別アバターをクリックした際もモーダルを閉じずに即座に枠線強調（`.selected`）が切り替わる。
  * デフォルトアバターには戻せない不可逆仕様。今後アバター画像を随時追加・拡張可能な動的設計を適用。

### ■ 盤面UIレイアウト（ハイブリッド2段レイアウト）
* **左右枠（240px拡張）**: 上家・下家の表示枠を拡張し、視認性とゆとりを確保。
* **位置ラベルの廃止と自然折り返し**: `【上家】` などの位置ラベルテキストを廃止（自分自身のみ `(あなた)` と表示）。プレイヤーネームは `word-break: break-all;` により10文字以内であれば「…」と省略されず1〜2行ですべて表示。
* **バッジ専用行の新設**: プレイヤーネーム直下に独立したバッジ表示専用コンテナを配置。バッジが複数付与されても名前表示を一切圧迫せず、残ターン数の丸囲み数字も欠けずにクリアに描画。

### ■ 本番 / 開発環境切り替えフラグ（IS_DEBUG）および本番環境の厳格制御
* **切り替え方法**: サーバー側の環境変数 `NODE_ENV=production` またはコード内フラグ `const IS_DEBUG = false;` により、本番環境と開発環境を1ステップで完全自動切り替え。
* **本番環境（`IS_DEBUG: false`）時の厳格制御**:
  1. **デバッグパネルの完全非表示**: 画面左上のデバッグパネル（`#debug-panel-root`）がDOM上で完全に非表示化される（初期ロード時のチラつきも完全防止）。
  2. **ボーナススキップの完全無効化**: 毎ターン必ず +3,000〜-3,000点選択ダイアログが表示される。
  3. **他者情報の非公開モード固定**: 相手の手札・防御カードは隠蔽され、防御成功で耐久残存時のみ可視化される本番ルールが強制適用される。
  4. **制限無視ドローの無効化**: 出現巡目・重複出現・重複所持・12Tクールダウン・順位別確率テーブルが厳格に適用される。
  5. **デバッグSocket通信のサーバー側完全遮断**: 不正な得点変更や強制ドローなどのSocketイベントがサーバー側で100%無視・遮断される。
* **開発環境（`IS_DEBUG: true`）時のデバッグUI仕様**:
  * 画面左上の「🛠 デバッグパネル」内に以下のテスト効率化機能を一括集約：
    * **「⚡ 強制参加＆スキップ開始」ボタン**: 接続中プレイヤーを P1〜P4 に強制割り当て、持ち点25,000点固定でドラフトをスキップして即座にメイン対戦を開始（進行中のリセットにも対応）。
    * **「制限無視ドロー」トグルスイッチ**: ON時は出現制限を全バイパスして完全ランダムドロー、OFF時は本番制限ルールを適用。
    * **「ボーナススキップ」スイッチ**: ターン開始時の得点選択ダイアログを自動スキップ（0点）。
    * **「他者情報公開/非公開」スイッチ**: 相手の手札枚数およびセット中防御カードの常時可視化切り替え。
    * **得点操作ボタン（+1k, -1k, +5k, -5k, Set）および強制ドローボタン**。
    * **「⚙ カード排出設定」モーダル呼び出しボタン**。

### ■ 他プレイヤー情報（手札・防御カード）の公開／非公開制御
* **公開モード（開発ON時）**: 自分以外のプレイヤーの手札枚数およびセット中防御カードを常時表示。
* **非公開モード（本番固定 / 開発OFF時）**: 相手プレイヤーの盤面UI上から「手札枚数」および「防御カード」の表示領域自体を完全に非表示化。
* **公開例外ルール**: 相手が防御カードで攻撃を無効化し、かつ耐久力（残り回数）が 1 以上残っている場合のみ、該当プレイヤーの盤面に「防御: 〇〇 (〇回)」表示領域が出現（可視化）する。

### ■ 手番進行インジケーター（手番完了ステータス）
* 各プレイヤー枠の右下エリアに、その巡目（ラウンド）の手番行動が完了したプレイヤーに対して **「済」バッジ** を表示。巡目終了時に自動でリセット。

### ■ ツールチップ表示位置の画面内自動補正
* 防御カード、ステータスバッジ、手札ホバー時のツールチップは、画面境界（ビューポート）を動的に検知して自動配置。
* 画面上端で見切れる場合は自動で下側に反転表示し、左右端ではみ出る場合も画面内に収まるようオフセット補正（長文テキストの見切れを完全防止）。

### ■ カードモーダルUI仕様
* **命中率リアルタイム計算**:
  * 攻撃対象や攻撃回数選択時、命中率（単体累積確率 / グループ順次判定確率 / ランダム選出確率）および暗闇状態による半減をリアルタイムに計算して表示。
  * 「青銅の盾セット」のパターンA（単体攻撃）において、最寄りの同点プレイヤーが複数存在する場合は、指定攻撃回数 $k$ と対象人数 $m$ に応じた実質命中率（$\min(1.0, k/m)$）をリアルタイムに計算して表示。
  * 「木の剣」「ショットガン」「グレネード」のグループ攻撃（下位全員）選択時は、順次判定による各下位対象への実質命中率（実効確率）を算出して表示。
* **影響対象の事前表示**: 各種範囲攻撃・特殊カード（地震・ダイヤの剣・ダークマター・煙幕）において、モーダル上に影響を受ける対象プレイヤー名をわかりやすく事前表示（※ダイヤの剣では選択不可プレイヤーも `P1（選択不可）` のように明示して事前表示）。
* **排他警告・ボタン非活性制御**: 「お守り大判」「ダークマター」では、同一ターン内の他カード使用状況および同種カード使用状況に応じた排他警告・ボタン非活性制御をリアルタイムに適用（アラートではなくモーダル上で最初から非活性化）。
* **時限爆弾の符号付き表示**: 時限爆弾の受け渡しモーダルでは、対象プレイヤーとの得点差を `+` / `-` / `±0` 符号付きで明瞭に表示。ターン終了時警告モーダルからの譲渡時は自動でターン終了へ移行。
* **カード説明文整形・動的耐久力表示**: モーダル内のカード説明文は改行コード（`\n`）やスタイル装飾を適切に反映して整形表示。セットカードの【残り回数】は手札・防御・プレビューすべてで動的に反映。
* **セット不可制御**: 防御カードとしてセットできないカード（ショットガン、地震、お守り小判、お守り大判等）は、モーダル上に防御セットボタンを表示しない。

---

## 5. ゲーム内テキスト（文言）の統一と管理規約

### ■ 適用範囲
本規約は、ゲーム内でプレイヤーに対して表示されるすべてのテキスト表現および文言に適用される。
1. 各種カードの説明文（desc）および名称
2. ステータスバッジ（無敵・ステロイド・煙幕・時限爆弾・選択不可等）のツールチップ表示テキスト
3. 各種モーダル、ダイアログ上の見出し、説明文、ボタンテキスト（攻撃実行・防御セット・受け渡し・破棄等）
4. ゲームログエリアに流れるシステムメッセージおよびアクションログ
5. その他、ゲームプレイ中に画面上に表示されるすべてのテキスト群

### ■ 文言の策定・管理主体
* ゲーム内に表示されるすべての文言は、**仕様策定者（担当者）が直接記述・管理**を行う。

### ■ 変更・改変に関する禁止事項
1. **無断変更・アレンジの禁止**:
   * 明確な指示または事前の協議がない限り、プログラム実装時およびUIデザイン作成時において、テキスト文言・ニュアンス・言い回しを開発者側で勝手に変更・要約・アレンジすることを禁止する。
2. **システム制約時の事前確認義務**:
   * レイアウト崩れや文字数オーバー等のシステム上の制約により、どうしても文言の変更・短縮が必要となる場合は、開発者が独自に判断して変更せず、**必ず事前に仕様策定者へ確認・提案を行い、了承を得た上で修正**を行うこと。