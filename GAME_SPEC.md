# カード対戦ゲーム 正式仕様書 (MASTER)
※本ファイルはゲーム実装の基準となる公式仕様書です。
※AIや開発者は本仕様に記述されたルールを最優先として実装・改修を行ってください。

---

## 1. 基本ルール・ゲーム進行

### ■ プレイヤー・勝敗
* **プレイ人数**: 4人 (P1, P2, P3, P4)
* **初期持ち点**: 25,000点 / 初期手札: 0枚
* **ゲーム終了条件**: 10巡（第10ラウンド終了時）完了時点で、最も得点が高いプレイヤーが勝者。

### ■ ターン順の決定ロジック（動的ターン順）
各巡目（ラウンド）において、まだその巡で行動していないプレイヤーの中から以下のように次の手番を決定する。
1. **1巡目かつ全員同点の場合**: P1 → P2 → P3 → P4 の昇順で行動。
2. **通常時**: 未行動のプレイヤーの中で **最も得点が高いプレイヤー** から順に行動。
   * 最高得点が同点の候補者が複数いる場合、その中からランダムで決定。

### ■ 1巡目の共通制限ルール（システム管理）
1. **攻撃対象制限**: 1巡目は、**自分より後に行動するプレイヤー** を攻撃対象として選択できない（UI上の選択肢からも除外）。
2. **カード効果無効**: 1巡目は、**自分より前に行動するプレイヤーが使用したカードの効果を受けない**（全体効果・特殊効果含む）。

### ■ ターンの流れ
1. **ターン開始時（ボーナス・ドローフェーズ）**:
   * +3,000点ボーナスの受取可否を選択（設定によりスキップ可能）。
   * 山札からカードを自動で1枚ドロー（※時限爆弾を引いた場合は手札に入らず即時強制発動）。
2. **メインフェーズ**:
   * 手札のカードを1枚クリックして使用、または防御カードへセット。
   * セット中の防御カードを外して攻撃、または破棄。
   * 自身が「時限爆弾状態」の場合、バッジをクリックして他者への受け渡しを試行可能。
   * 「ターン終了」ボタンで手番を終了（未挑戦の時限爆弾と対象がある場合は警告確認ダイアログが表示）。
3. **手札調整フェーズ（手札オーバー時）**:
   * ターン終了時に手札が2枚以上ある場合、1枚以下になるまで捨てるカードを選択。

---

## 2. 状態変化（バフ・デバフ）と防御仕様

### ■ 状態変化一覧
| 状態名 | 種類 | 公開範囲 | タイマー減少タイミング | 効果概要 |
| :--- | :--- | :--- | :--- | :--- |
| **無敵（無敵アーマー）** | バフ | **初期非公開**<br>（攻撃防御で全公開） | **誰かのターンが1回終了する毎に -1** (初期値: 4T) | あらゆる攻撃カードの効果を受けない。<br>【解除時】+1000点 ＆ 条件を満たす相手へ50%で手札・防御全破棄 / -3000点 / 選択不可付与。 |
| **ステロイド** | バフ | **初期非公開**<br>（攻撃防御で全公開） | **誰かのターンが1回終了する毎に -1** (初期値: 4T) | 一部を除く攻撃カードの効果を受けない（大災害を受けると即解除）。<br>【解除時】+1000点 ＆ 条件を満たす相手（ステロイド除く）へ50%で手札・防御全破棄 / -3000点 / 選択不可付与。 |
| **無敵（ダークマター）** | バフ | **常時公開** | **使用者の次の自ターン開始時に解除** | あらゆる攻撃カードの効果を受けない。 |
| **暗闇（煙幕）** | デバフ | **常時公開** | **付与されたプレイヤー自身がターン終了する毎に -1** | 攻撃カード使用時の命中率が半減する。<br>継続ターン: 対象が1位なら2T、2位〜4位なら1T。 |
| **選択不可** | 状態 | **常時公開** | **付与されたプレイヤー以外の誰かのターンが1回終了する毎に -1** (初期値: 2) | 攻撃カードの対象として選択できなくなる。 |
| **時限爆弾状態** | 特殊状態 | **常時公開** | **誰かのターンが1回終了する毎に -1** (初期値: 8T) | 自ターン開始毎に+1000点。±3000点差の相手に50%で受け渡し可能。無敵・ステロイドで消滅。<br>【カウント0時】爆発して -6000点 / 手札・防御全破棄 / 選択不可(2T上書き)付与。 |

### ■ バフバッジの公開範囲・動的切り替え仕様
* **無敵アーマー／ステロイド**:
  * **初期状態**: 使用者本人にのみバッジを表示（他プレイヤーからは非公開）。カード使用ログも他者には伏せられる。
  * **公開条件**: 該当のバフ効果によって攻撃を1度でも防いだ（無効化した）瞬間。
  * **変化後**: 攻撃を防いだ時点から、全プレイヤーに対してステータスバッジ（残ターン数含む）が可視化される。
  * **解除時**: 効果終了とともに非表示化・リセットされる。
* **時限爆弾状態**:
  * 常に全プレイヤーに公開され、残りターン数がバッジとして表示される。

### ■ 防御効果の処理優先順位（判定順序）
自身にバフ（無敵アーマー／ステロイド／ダークマター）と防御カードが両方存在している状態で攻撃を受けた場合、**セットされている防御カードの消費が最優先**される。
1. **第1優先（防御カード）**: セット中の防御カードで使用回数を1消費して無効化（※防御カードとしてセットされた「木の剣／木の剣セット／グレネード」は高得点者から受けた攻撃の場合、防御効果が発動せず貫通。また「ショットガン」「グレネード（攻撃）」などの貫通攻撃は防御カードを無視して貫通する）。
2. **第2優先（バフ状態）**: 防御カードがない、または防御を貫通された場合、「無敵」「ステロイド」状態による無効化判定を行う（※ここで無効化した場合、バッジが全プレイヤーへ公開される）。
3. **第3優先（ダメージ・効果適用）**: いずれの防御も行われなかった場合、得点ダメージおよび選択不可などのデバフを適用。

### ■ ターン終了時のタイマー減算順序（厳密仕様）
ターン終了時は、以下の**2段階処理**でタイマーを更新する。
1. **ステップ1（既存タイマーの減算）**:
   * 現在手番を終えたプレイヤー自身の「暗闇」タイマーを -1。
   * 手番を終えたプレイヤー**以外**の既存の「選択不可」タイマーを -1。
2. **ステップ2（バフ解除・ペナルティ判定・時限爆弾処理）**:
   * 「無敵アーマー」「ステロイド」「時限爆弾」のタイマーを -1。
   * 無敵アーマー／ステロイドが 0 になった場合、解除時効果（+1000点＆相手へのペナルティ判定）を実行。
   * 時限爆弾が 0 になった場合、爆発ペナルティ（-6000点、手札防御全破棄、選択不可2T上書き）を実行。
   * ※解除ペナルティで付与された「選択不可（2）」は、ステップ1の減算完了後に付与されるため、**付与されたターン終了時に即座に減算されることはなく、確実に2回分の手番終了まで持続する**。

---

## 3. カード一覧と効果詳細

### ① 木の盾 (WOOD_SHIELD)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * 対象: 「自分と同点以上の全員」または「自分より下位の全員」
  * 命中率: `max(0, 1 - |得点差| / 10000)` （点差が近い順に判定）
  * 効果: 命中した1名に -3000点 ＆ 選択不可付与。命中または無効化された時点で攻撃中断・カード破棄。
* **防御時**: 相手からの攻撃を1度無効化（使用回数: 1回）。

### ② 木の盾セット (WOOD_SHIELD_SET)
* **分類**: 攻撃 / 防御 (使用回数: 3回)
* **重複制限**: 手札または防御にセット中の場合、山札から新たに引けない。
* **攻撃時**:
  * 対象: 「自分と同点以上の全員」または「自分より下位の全員」
  * 攻撃回数（1〜残り使用回数）を指定してグループ攻撃。
  * 効果: 命中した相手に -3000点 ＆ 選択不可付与。ミス・無効化・命中に関わらず指定回数分判定を継続可能（無敵・ステロイドでの無効化時は中断）。
* **防御時**: 相手からの攻撃を無効化（最大3回まで）。

### ③ 青銅の盾 (BRONZE_SHIELD)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * **パターンA（単体対象）**:
    * 対象: 自分との得点差が **0点以上 +10,000点以内** の中で、最も得点差が近いプレイヤー1名（同点差が複数の場合はランダム1名選出）。
    * 命中率: **必中（100%）**（「暗闇状態」の影響を受けない。防御カード・無敵・ステロイドでの無効化は通常通り適用）。
    * UI表示: 該当人数に応じた実質選出命中率（`100% ÷ 対象人数`）を表示。
  * **パターンB（グループ対象）**:
    * 対象: **自分より得点が低いプレイヤー全員**（点差が近い順に順次判定）。
    * 命中率: `max(0, 1 - |得点差| / 5000)`（「暗闇状態」時は半減）。
    * 処理: 命中・防御カード無効化・無敵／ステロイド無効化のいずれかが1度でも発生した時点で即座に攻撃終了（中断）。
  * 効果: 命中した相手に -3000点 ＆ 選択不可付与。
* **防御時**: 相手からの攻撃を1度無効化（使用回数: 1回）。

### ④ 木の剣 (WOOD_SWORD)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * 単体対象: 自分と同点以上かつ **+5,000点以内** のプレイヤー1名（命中率: 50%）
  * グループ対象: **自分より下位の全員**（点差が近い順に判定、命中率: 50%）
  * 効果: 命中した相手に -3000点 ＆ 選択不可付与。
* **防御時**: 攻撃を1度無効化。**ただし、自分より得点が高いプレイヤーからの攻撃は無効化不可。**

### ⑤ 木の剣セット (WOOD_SWORD_SET)
* **分類**: 攻撃 / 防御 (使用回数: 3回)
* **重複制限**: 手札または防御にセット中の場合、山札から新たに引けない。
* **攻撃時**:
  * 攻撃回数（1〜残り使用回数）を指定して使用。
  * **パターンA（単体対象）**:
    * 対象: 自分と同点以上かつ **+5,000点以内** のプレイヤー1名（命中率: 50%）
    * 処理: 1回ごとに使用回数を1消費。命中しない・防御カードで無効化された場合、回数が残っていれば同じ対象へ連続攻撃を継続。命中時は -3000点 ＆ 選択不可付与し中断。
  * **パターンB（グループ対象）**:
    * 対象: **自分より下位の全員**（点差が近い順に順次判定、命中率: 50%）
    * 処理: 1周判定ごとに使用回数を1消費。命中・防御無効化・全員ミスに関わらず、指定回数・残り使用回数・対象が残っていれば点差が近い順に攻撃を継続。
    * 中断条件: 回数終了、対象消失、または防御カード以外（無敵・ステロイド状態等）で無効化された場合に中断。
  * 効果: 命中した相手に -3000点 ＆ 選択不可付与。
* **防御時**: 攻撃を無効化（最大3回まで）。**ただし、自分より得点が高いプレイヤーからの攻撃は無効化不可。**

### ⑥ ショットガン (SHOTGUN)
* **分類**: 攻撃（※防御カードとしてはセット不可）
* **貫通仕様**: 相手のセット中防御カードを完全に貫通してダメージを与える（相手の防御カードの耐久回数は減少しない）。
* **攻撃時**:
  * **パターンA（単体対象）**:
    * 対象: 自分との得点差が **0〜+5,000点以内** のプレイヤー1名（命中率: 50%）
    * 処理: 指定した対象1名に貫通攻撃を行う。
  * **パターンB（グループ対象）**:
    * 対象: **自分より得点が低いプレイヤー全員**（点差が近い順に順次判定、命中率: 50%）
    * 処理: 点差順に貫通攻撃を行い、命中または「無敵／ステロイド状態」による無効化のいずれかが1度でも発生した時点で即座に攻撃終了（中断）。
  * 効果: 命中した相手に -3000点 ＆ 選択不可付与。

### ⑦ グレネード (GRENADE)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * **貫通仕様**: 相手のセット中防御カードを貫通して着弾・爆発が適用される。
  * **パターンA（単体対象）**: 自分との得点差が **0〜+5,000点以内** のプレイヤー1名（命中率: 50%）。
  * **パターンB（グループ対象）**: 自分との得点差が **-1〜-5,000点以内** のプレイヤー全員（点差が近い順に順次判定、命中率: 80%）。命中または無敵等による無効化が発生した時点で攻撃即時終了。
  * **スプラッシュ爆発（巻添え効果）**:
    * 命中した対象、および**その対象と得点差が±1000点以内のプレイヤー全員（自分を含む）に -5000点、手札・防御全破棄、選択不可状態（2ターン）**を適用。
    * 対象が「無敵」「ステロイド」で直接攻撃を防いだ場合でも、±1000点以内のスプラッシュ爆発は通常通り誘爆・発生する。
    * 「無敵」「ステロイド」「選択不可」状態のプレイヤーはダメージ・手札破棄・選択不可は無効だが、セット中の防御カードのみ破棄される。
* **防御時**:
  * 相手からの攻撃を1度無効化（使用回数: 1回）。**ただし、自分より得点が高いプレイヤーからの攻撃は無効化不可。**
  * **カウンター反撃**: 無効化成功時、**自分との得点差が 0〜-1000点以内の相手全員（自分・格上除く）に -5000点、手札・防御全破棄、選択不可状態（2ターン）**を適用（無敵・ステロイド・選択不可には防御破棄のみ）。

### ⑧ ダイヤの剣 (DIAMOND_SWORD)
* **分類**: 攻撃
* **命中率**: 必中（「暗闇状態」の影響を受けない）
* **対象**: 現時点で **1位のプレイヤー、および1位との得点差が ±1,000点以内のプレイヤー全員**（使用者自身が該当する場合も対象に含まれる）。
* **効果**:
  * 対象全員の手札とセットされている防御カードをすべて破棄する。
  * 対象全員の得点を **-5,000点** する。
  * ダメージを受けたプレイヤーに **選択不可状態（2巡）** を付与する。
* **特記事項（例外処理）**:
  * 対象が「無敵状態」「ステロイド状態」「選択不可状態」の場合、得点ダメージ（-5000点）および選択不可状態の付与は発生せず、**セットされている防御カードの破棄のみ** が適用される。
  * 対象が手札に「お守り小判」を所持している場合、お守り小判が自動消費され、このカードの効果がすべて無効化される。

### ⑨ 地震 (EARTHQUAKE)
* **分類**: 攻撃
* **演出**: 発動時に全画面カットイン演出（「地震発動！」）が発生。
* **対象**: **自分と同点以上の相手全員**（使用者は除外）
* **不発条件**: 対象条件を満たすプレイヤーが0名の場合、カード効果は不発となる。
* **効果**:
  * 対象全員に対し、個別に 50% の確率で **-1,000点** / 50% の確率で **-3,000点** のダメージを与える。
  * 対象全員の手札およびセットされている防御カードをすべて破棄する。
  * **選択不可状態の付与（貫通・上書きルール）**:
    * すでに対象が「選択不可状態」であっても効果・デバフを貫通して適用する。
    * 対象の選択不可状態を **「2ターン固定」へと上書き（更新）** する。
* **特記事項（例外処理）**:
  * 対象が「無敵状態」「ステロイド状態」の場合、ダメージ・カード破棄・選択不可状態付与のすべての効果が無効化される。

### ⑩ 時限爆弾 (TIME_BOMB)
* **分類**: 特殊
* **ドロー時挙動（即時強制発動）**:
  * 山札から引いた瞬間、手札に入らず自動で強制発動（「時限爆弾出現！」のカットイン演出）。
  * 自身の得点を **+1,000点** し、自分に **「時限爆弾状態（8ターン）」** を付与する（※防御カードのセット状態は維持される）。
* **「時限爆弾状態」の持続効果**:
  * ターン開始時効果: 保持者の自ターン開始時、保持者の得点を **+1,000点** する（※ドローしたターンは重複防止のため除外）。
  * 他バフによる消滅: 保持者が「無敵状態」または「ステロイド状態」になった場合、時限爆弾状態は即座に消滅する（爆発ペナルティなし）。
* **受け渡し（譲渡）処理**:
  * 自分のターン中、バッジをクリックして自分との得点差が **±3,000点以内** のプレイヤー（選択不可状態含む。無敵・ステロイドは除外）へ受け渡しを試行可能。
  * 確率: **50%**。成功時は対象へ残りターン数を引き継いで移動。失敗時はそのターン中再試行不可。
  * 安全装置: 未挑戦かつ譲渡対象が存在する場合、ターン終了時に確認モーダルが開き「無視してターン終了」か「受け渡し」を選択可能。
* **カウント0時の爆発ペナルティ**:
  * 8ターン経過しカウントが0になった瞬間、保持者に **-6,000点**、**手札・防御カード全破棄**、**選択不可状態（2ターン上書き）** を適用。

### ⑪ お守り小判 (OMAMORI_KOBAN)
* **分類**: 特殊
* **通常効果**: 使用すると自分の得点を **+3,000点** する。使用後破棄。
* **自動発動効果（ダイヤの剣カウンター）**:
  * 手札に所持している状態で自分が「ダイヤの剣」の対象になった時、手札のこのカードが自動で1枚消費される。
  * その「ダイヤの剣」の効果（ダメージ、手札・防御カード破棄、選択不可付与）を完全に無効化する。
  * 自動消費時にも、通常効果と同様に自分の得点が **+3,000点** される。

### ⑫ 大災害 (DISASTER)
* **分類**: 攻撃 (命中率: 100%)
* **対象**: 使用者以外の全員
* **効果**:
  * 手札およびセット中防御カードを全破棄。
  * 順位に応じたダメージ: 1位: -6000点 / 2位: -4000点 / 3位: -2000点 / 4位: -1000点。
  * ダメージを受けたプレイヤーに選択不可付与。
  * ※ステロイド状態の相手は大災害を防御するが、ステロイド状態が即時強制解除される。

### ⑬ 無敵アーマー (INVINCIBLE_ARMOR)
* **分類**: 特殊
* **公開範囲**: **初期は本人のみ表示**（攻撃を1度防ぐと全プレイヤーに公開）
* **効果**: 使用から合計4ターン経過まで「無敵状態」になる。解除時に+1000点＆相手へペナルティ判定。

### ⑭ ダークマター (DARK_MATTER)
* **分類**: 特殊
* **効果**:
  * 自身の得点を +5000点。
  * 次の自分ターン開始時まで「無敵状態」になる。
  * 使用直後の同点・逆転相手（無敵・選択不可除く）へ50%で手札・防御全破棄＆-3000点＆選択不可付与。

### ⑮ ステロイド (STEROID)
* **分類**: 特殊
* **公開範囲**: **初期は本人のみ表示**（攻撃を1度防ぐと全プレイヤーに公開）
* **効果**: 使用から合計4ターン経過まで「ステロイド状態」になる。解除時に+1000点＆相手へペナルティ判定。

### ⑯ 煙幕 (SMOKE_SCREEN)
* **分類**: 攻撃
* **対象**: 自分以上の得点を持つ相手全員（該当者なしの場合は自身）
* **効果**:
  * 対象の得点を -1000点 ＆ 「暗闇状態」付与。
  * 継続ターン数: 対象が1位なら2T、2位〜4位なら1T。
  * 自身への跳ね返り時、自身が無敵・ステロイド状態なら無効化。

---

## 4. UI・表示仕様
* **バフ/デバフバッジ**:
  * 無敵アーマー・ダークマター・ステロイド・煙幕・時限爆弾はミニカードイラスト＋残ターン数バッジで表示。
  * 無敵アーマー・ステロイドは、攻撃を防ぐまでは使用者本人にのみバッジが表示され、防いだ時点から全プレイヤーへ公開表示される。
  * マウスオーバー時に専用ツールチップ（効果説明・改行・装飾タグを正しく反映）を表示。
  * 「選択不可」は `[選択不可:X]` テキスト形式を維持。
* **カードモーダルUI**:
  * 攻撃対象や攻撃回数選択時、命中率（単体累積確率 / グループ順次判定確率 / ランダム選出確率）および暗闇状態による半減をリアルタイムに計算して表示。
  * ツールチップおよびモーダル内のカード説明文は改行コード（`\n`）やスタイル装飾を適切に反映して整形表示。
  * 防御カードとしてセットできないカード（ショットガン、地震等）は、モーダル上に防御セットボタンを表示しない。
* **画面描画・演出の安定性**:
  * 得点変動演出（`持ち点: 〇〇点 ±〇〇点 → 〇〇点`）は、局所的なスコア要素更新と差分検知タイマーにより最低3.5秒間は画面上に保持され、目視できるように維持される。
  * 手札カードホバー時の不要なDOM再描画・点滅（チラつき）を防止する安定化レイアウトを適用。