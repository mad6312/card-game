# カード対戦ゲーム 正式仕様書 (MASTER)
※本ファイルはゲーム実装の基準となる公式仕様書です。
※AIや開発者は本仕様に記述されたルールを最優先として実装・改修を行ってください。

---

## 1. 基本ルール・ゲーム進行

### ■ プレイヤー・勝敗
* **プレイ人数**: 4人 (P1, P2, P3, P4)
* **初期持ち点**: 25,000点 / 初期手札: 0枚
* **ゲーム終了条件**: 10巡（第10ラウンド終了時）完了時点で、最も得点が高いプレイヤーが勝者。

### ■ ターン順の決定ロジック（動的ターン順）
各巡目（ラウンド）において、まだその巡で行動していないプレイヤーの中から以下のように次の手番を決定する。
1. **1巡目かつ全員同点の場合**: P1 → P2 → P3 → P4 の昇順で行動。
2. **通常時**: 未行動のプレイヤーの中で **最も得点が高いプレイヤー** から順に行動。
   * 最高得点が同点の候補者が複数いる場合、その中からランダムで決定。

### ■ 1巡目の共通制限ルール（システム管理）
1. **攻撃対象制限**: 1巡目は、**自分より後に行動するプレイヤー** を攻撃対象として選択できない（UI上の選択肢からも除外）。
2. **カード効果無効**: 1巡目は、**自分より前に行動するプレイヤーが使用したカードの効果を受けない**（全体効果・特殊効果含む）。

### ■ ターンの流れ
1. **ターン開始時（ボーナス・ドローフェーズ）**:
   * +3,000点ボーナスの受取可否を選択（設定によりスキップ可能）。
   * 山札からカードを自動で1枚ドロー（※時限爆弾を引いた場合は手札に入らず即時強制発動）。
2. **メインフェーズ**:
   * 手札のカードを1枚クリックして使用、または防御カードへセット。
   * セット中の防御カードを外して攻撃、または破棄。
   * 自身が「時限爆弾状態」の場合、バッジをクリックして他者への受け渡しを試行可能（自ターン継続）。
   * 「ターン終了」ボタンで手番を終了（未挑戦の時限爆弾と対象がある場合は安全装置モーダルが表示）。
3. **手札調整フェーズ（手札オーバー時）**:
   * ターン終了時に手札が2枚以上ある場合、1枚以下になるまで捨てるカードを選択。

---

## 2. 状態変化（バフ・デバフ）と防御仕様

### ■ 状態変化一覧
| 状態名 | 種類 | 公開範囲 | タイマー減少タイミング | 効果概要 |
| :--- | :--- | :--- | :--- | :--- |
| **無敵（無敵アーマー）** | バフ | **初期非公開**<br>（攻撃防御で全公開） | **誰かのターンが1回終了する毎に -1** (初期値: 4T) | あらゆる攻撃カードの効果を受けない。<br>【解除時】+1,000点 ＆ 条件を満たす相手へ50%で手札・防御全破棄 / -3,000点 / 選択不可付与。 |
| **ステロイド** | バフ | **初期非公開**<br>（攻撃防御で全公開） | **誰かのターンが1回終了する毎に -1** (初期値: 4T) | 一部を除く攻撃カードの効果を受けない（大災害を受けると即解除）。<br>【解除時】+1,000点 ＆ 条件を満たす相手（ステロイド除く）へ50%で手札・防御全破棄 / -3,000点 / 選択不可付与。 |
| **無敵（ダークマター）** | バフ | **常時公開** | **使用者の次の自ターン開始時に解除** | あらゆる攻撃カードの効果を受けない。 |
| **暗闇（煙幕）** | デバフ | **常時公開** | **付与されたプレイヤー自身がターン終了する毎に -1** | 攻撃カード使用時の命中率が半減する。<br>継続ターン: 対象が1位なら2T、2位〜4位なら1T。 |
| **選択不可** | 状態 | **常時公開** | **付与されたプレイヤー以外の誰かのターンが1回終了する毎に -1** (初期値: 2) | 攻撃カードの対象として選択できなくなる。 |
| **時限爆弾状態** | 特殊状態 | **常時公開** | **誰かのターンが1回終了する毎に -1** (初期値: 8T) | 自ターン開始毎に+1,000点。±3,000点差の相手に50%で受け渡し可能。無敵・ステロイドで消滅。<br>【カウント0時】爆発して -6,000点 / 手札・防御全破棄 / 選択不可(2T上書き)付与。 |

### ■ バフバッジの公開範囲・動的切り替え仕様
* **無敵アーマー／ステロイド**:
  * **初期状態**: 使用者本人にのみバッジを表示（他プレイヤーからは非公開）。カード使用ログも他者には伏せられる。
  * **公開条件**: 該当のバフ効果によって攻撃を1度でも防いだ（無効化した）瞬間。
  * **変化後**: 攻撃を防いだ時点から、全プレイヤーに対してステータスバッジ（残ターン数含む）が可視化される。
  * **解除時**: 効果終了とともに非表示化・リセットされる。
* **時限爆弾状態**:
  * 常に全プレイヤーに公開され、残りターン数がバッジとして表示される。

### ■ 防御効果の処理優先順位（判定順序）
自身にバフ（無敵アーマー／ステロイド／ダークマター）と防御カードが両方存在している状態で攻撃を受けた場合、**セットされている防御カードの消費が最優先**される。
1. **第1優先（防御カード）**: セット中の防御カードで使用回数を1消費して無効化（※防御カードとしてセットされた「木の剣／木の剣セット／グレネード」は高得点者から受けた攻撃の場合、防御効果が発動せず貫通。また「ショットガン」「グレネード（攻撃）」などの貫通攻撃は防御カードを無視して貫通する）。
2. **第2優先（バフ状態）**: 防御カードがない、または防御を貫通された場合、「無敵」「ステロイド」状態による無効化判定を行う（※ここで無効化した場合、バッジが全プレイヤーへ公開される）。
3. **第3優先（ダメージ・効果適用）**: いずれの防御も行われなかった場合、得点ダメージおよび選択不可などのデバフを適用。

### ■ ターン終了時のタイマー減算順序（厳密仕様）
ターン終了時は、以下の**2段階処理**でタイマーを更新する。
1. **ステップ1（既存タイマーの減算）**:
   * 現在手番を終えたプレイヤー自身の「暗闇」タイマーを -1。
   * 手番を終えたプレイヤー**以外**の既存の「選択不可」タイマーを -1。
2. **ステップ2（バフ解除・ペナルティ判定・時限爆弾処理）**:
   * 「無敵アーマー」「ステロイド」「時限爆弾」のタイマーを -1。
   * 無敵アーマー／ステロイドが 0 になった場合、解除時効果（+1,000点＆相手へのペナルティ判定）を実行。
   * 時限爆弾が 0 になった場合、爆発ペナルティ（-6,000点、手札防御全破棄、選択不可2T上書き）を実行。
   * ※解除ペナルティで付与された「選択不可（2）」は、ステップ1の減算完了後に付与されるため、**付与されたターン終了時に即座に減算されることはなく、確実に2回分の手番終了まで持続する**。

---

## 3. カード一覧と効果詳細

### ■ セットカード重複所持制限（共通排他制御）
* **対象カード**: 「木の盾セット」「青銅の盾セット」「木の剣セット」
* **ルール**: 上記3種のいずれかを手札に所持しているか、または防御カードとしてセットしているプレイヤーは、山札からこれら3種のセットカードを新たに引くことができない（排他制御）。

---

### ① 木の盾 (WOOD_SHIELD)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * 対象: 「自分と同点以上の全員」または「自分より下位の全員」
  * 命中率: `max(0, 1 - |得点差| / 10000)` （点差が近い順に判定）
  * 効果: 命中した1名に -3,000点 ＆ 選択不可付与。命中または無効化された時点で攻撃中断・カード破棄。
  * **演出（全画面突進カットイン）**: 発動時に全画面対峙カットインが起動。左側の攻撃ユニットが右側の対象プレイヤー群（点差順整列）へ向かって順次突進し、回避・命中・防御・無敵判定をシームレスに再生。
* **防御時**: 相手からの攻撃を1度無効化（使用回数: 1回）。

### ② 木の盾セット (WOOD_SHIELD_SET)
* **分類**: 攻撃 / 防御 (使用回数: 3回)
* **重複制限**: 3種セットカード重複所持制限の対象。
* **攻撃時**:
  * 対象: 「自分と同点以上の全員」または「自分より下位の全員」
  * 攻撃回数（1〜残り使用回数）を指定してグループ攻撃。
  * 効果: 命中した相手に -3,000点 ＆ 選択不可付与。ミス・無効化・命中に関わらず指定回数分判定を継続可能（無敵・ステロイドでの無効化時は中断）。
  * **演出（全画面連撃カットイン）**: 
    * 指定攻撃回数（1〜3回）に応じたラウンド制連撃ループを再生。
    * 命中した対象は画面外へ吹き飛んで離脱。次撃は残存している対象に対して初期位置から再度突進。
    * 全員回避時は指定回数を1消費して次撃へ継続。無敵・ステロイドでの無効化時または生存対象消失時は即座に連撃中断・演出終了。
* **防御時**: 相手からの攻撃を無効化（最大3回まで）。

### ③ 青銅の盾 (BRONZE_SHIELD)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * **パターンA（単体対象）**:
    * 対象: 自分との得点差が **0点以上 +10,000点以内** の中で、最も得点差が近いプレイヤー1名（同点差が複数の場合はランダム1名選出）。
    * 命中率: **必中（100%）**（「暗闇状態」の影響を受けない。防御カード・無敵・ステロイドでの無効化は通常通り適用）。
    * UI表示: 該当人数に応じた実質選出命中率（`100% ÷ 対象人数`）を表示。
  * **パターンB（グループ対象）**:
    * 対象: **自分より得点が低いプレイヤー全員**（点差が近い順に順次判定）。
    * 命中率: `max(0, 1 - |得点差| / 5000)`（「暗闇状態」時は半減）。
    * 処理: 命中・防御カード無効化・無敵／ステロイド無効化のいずれかが1度でも発生した時点で即座に攻撃終了（中断）。
  * 効果: 命中した相手に -3,000点 ＆ 選択不可付与。
  * **演出（全画面突進カットイン）**: 単体・グループ攻撃ともに、青銅の盾専用の全画面突進カットイン（点差順整列）を再生。
* **防御時**: 相手からの攻撃を1度無効化（使用回数: 1回）。

### ④ 青銅の盾セット (BRONZE_SHIELD_SET)
* **分類**: 攻撃 / 防御 (使用回数: 3回)
* **重複制限**: 3種セットカード重複所持制限の対象。
* **攻撃時**:
  * 攻撃回数（1〜残り使用回数）を指定して使用。
  * **パターンA（単体攻撃）**:
    * 対象: 自分との得点差が **0点以上 +10,000点以内** の中で、最も得点差が近いプレイヤー（攻撃ごとに動的再選定）。
    * 命中率: **必中（100%）**（「暗闇状態」の影響を受けない）。
    * **動的選定・連撃処理**:
      * 1回攻撃するごとに現在の盤面から対象を再選定。該当する最寄りの点差のプレイヤーが複数いる場合は、攻撃1回ごとにランダムで決定。
      * 攻撃が命中し「選択不可状態」となったプレイヤーは、次回以降の攻撃対象から即座に除外される。
      * 攻撃の結果が「命中（-3,000点＆選択不可付与）」「ミス」「防御カードでの無効化」のいずれであっても、指定攻撃回数／残り使用回数と有効な対象が存在する限り、連撃を継続する。
    * **中断・終了条件**: 回数終了、対象消失（全員が選択不可等）、または防御カード以外（無敵・ステロイド状態等）で無効化された場合に即時終了。
  * **パターンB（グループ対象）**:
    * 対象: **自分より得点が低いプレイヤー全員**（点差が近い順に順次判定）。
    * 命中率: `max(0, 1 - |得点差| / 5000)`（「暗闇状態」時は半減）。
    * 処理: 1周判定ごとに使用回数を1消費。命中・防御無効化・全員ミスに関わらず、指定回数・残り回数・対象が残っていれば点差が近い順に2周目以降の攻撃を継続。
    * 中断条件: 回数終了、対象消失、または無敵・ステロイド状態で無効化された場合に中断。
  * 効果: 命中した相手に -3,000点 ＆ 選択不可付与。
* **防御時**: 相手からの攻撃を無効化（最大3回まで）。

### ⑤ 木の剣 (WOOD_SWORD)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * 単体対象: 自分と同点以上かつ **+5,000点以内** のプレイヤー1名（命中率: 50%）
  * グループ対象: **自分より下位の全員**（点差が近い順に判定、命中率: 50%）
  * 効果: 命中した相手に -3,000点 ＆ 選択不可付与。
  * **演出（全画面突進カットイン）**: 単体・グループ攻撃ともに、木の剣専用の全画面突進カットイン（点差順整列）を再生。
* **防御時**: 攻撃を1度無効化。**ただし、自分より得点が高いプレイヤーからの攻撃は無効化不可。**

### ⑥ 木の剣セット (WOOD_SWORD_SET)
* **分類**: 攻撃 / 防御 (使用回数: 3回)
* **重複制限**: 3種セットカード重複所持制限の対象。
* **攻撃時**:
  * 攻撃回数（1〜残り使用回数）を指定して使用。
  * **パターンA（単体対象）**:
    * 対象: 自分と同点以上かつ **+5,000点以内** のプレイヤー1名（命中率: 50%）
    * 処理: 1回ごとに使用回数を1消費。命中しない・防御カードで無効化された場合、回数が残っていれば同じ対象へ連続攻撃を継続。命中時は -3,000点 ＆ 選択不可付与し中断。
  * **パターンB（グループ対象）**:
    * 対象: **自分より下位の全員**（点差が近い順に順次判定、命中率: 50%）
    * 処理: 1周判定ごとに使用回数を1消費。命中・防御無効化・全員ミスに関わらず、指定回数・残り使用回数・対象が残っていれば点差が近い順に攻撃を継続。
    * 中断条件: 回数終了、対象消失、または防御カード以外（無敵・ステロイド状態等）で無効化された場合に中断。
  * 効果: 命中した相手に -3,000点 ＆ 選択不可付与。
* **防御時**: 攻撃を無効化（最大3回まで）。**ただし、自分より得点が高いプレイヤーからの攻撃は無効化不可。**

### ⑦ ショットガン (SHOTGUN)
* **分類**: 攻撃（※防御カードとしてはセット不可）
* **貫通仕様**: 相手のセット中防御カードを完全に貫通してダメージを与える（相手の防御カードの耐久回数は減少しない）。
* **攻撃時**:
  * **パターンA（単体対象）**:
    * 対象: 自分との得点差が **0〜+5,000点以内** のプレイヤー1名（命中率: 50%）
    * 処理: 指定した対象1名に貫通攻撃を行う。
  * **パターンB（グループ対象）**:
    * 対象: **自分より得点が低いプレイヤー全員**（点差が近い順に順次判定、命中率: 50%）
    * 処理: 点差順に貫通攻撃を行い、命中または「無敵／ステロイド状態」による無効化のいずれかが1度でも発生した時点で即座に攻撃終了（中断）。
  * 効果: 命中した相手に -3,000点 ＆ 選択不可付与。
  * **演出（全画面射撃・貫通カットイン）**: 
    * 攻撃側からマズルフラッシュとリコイル反動とともに高速の銃弾トレーサー（光線弾）が発射。
    * 防御カードをセット中の相手には「貫通！」バッジが出現し、防御カードごと打ち抜かれて画面外へ吹き飛ぶド派手な貫通演出を再生。
    * 複数対象時、回避された場合は銃弾がそのまま減速せず次の対象へ直進。

### ⑧ グレネード (GRENADE)
* **分類**: 攻撃 / 防御
* **攻撃時**:
  * **貫通仕様**: 相手のセット中防御カードを貫通して着弾・爆発が適用される。
  * **パターンA（単体対象）**: 自分との得点差が **0〜+5,000点以内** のプレイヤー1名（命中率: 50%）。
  * **パターンB（グループ対象）**: 自分との得点差が **-1〜-5,000点以内** のプレイヤー全員（点差が近い順に順次判定、命中率: 80%）。命中または無敵等による無効化が発生した時点で攻撃即時終了。
  * **スプラッシュ爆発（巻添え効果）**:
    * 命中した対象、および**その対象と得点差が±1,000点以内のプレイヤー全員（自分を含む）に -5,000点、手札・防御全破棄、選択不可状態（2ターン）**を適用。
    * 対象が「無敵」「ステロイド」で直接攻撃を防いだ場合でも、±1,000点以内のスプラッシュ爆発は通常通り誘爆・発生する。
    * 「無敵」「ステロイド」「選択不可」状態のプレイヤーはダメージ・手札破棄・選択不可は無効だが、セット中の防御カードのみ破棄される。
* **防御時**:
  * 相手からの攻撃を1度無効化（使用回数: 1回）。**ただし、自分より得点が高いプレイヤーからの攻撃は無効化不可。**
  * **カウンター反撃**: 無効化成功時、**自分との得点差が 0〜-1,000点以内の相手全員（自分・格上除く）に -5,000点、手札・防御全破棄、選択不可状態（2ターン）**を適用（無敵・ステロイド・選択不可には防御破棄のみ）。

### ⑨ ダイヤの剣 (DIAMOND_SWORD)
* **分類**: 攻撃
* **命中率**: 必中（「暗闇状態」の影響を受けない）
* **対象**: 現時点で **1位のプレイヤー、および1位との得点差が ±1,000点以内のプレイヤー全員**（使用者自身が該当する場合も対象に含まれる）。
* **UI表示**: モーダル上に攻撃対象となるプレイヤー名（1位および±1,000点以内）を事前表示。
* **効果**:
  * 対象全員の手札とセットされている防御カードをすべて破棄する。
  * 対象全員の得点を **-5,000点** する。
  * ダメージを受けたプレイヤーに **選択不可状態（2巡）** を付与する。
* **特記事項（例外処理）**:
  * 対象が「無敵状態」「ステロイド状態」「選択不可状態」の場合、得点ダメージ（-5,000点）および選択不可状態の付与は発生せず、**セットされている防御カードの破棄のみ** が適用される。
  * 対象が手札に「お守り大判」「お守り小判」「お守り小判セット」を所持している場合、自動で消費・回避判定が行われ、このカードの効果がすべて無効化される。

### ⑩ 地震 (EARTHQUAKE)
* **分類**: 攻撃
* **演出**: 発動時に全画面カットイン演出（「地震発動！」）が発生。
* **対象**: **自分と同点以上の相手全員**（使用者は除外）
* **UI表示**: モーダル上に攻撃対象となる同点以上の相手プレイヤー名を事前表示。対象が0名の場合は不発警告を表示。
* **不発条件**: 対象条件を満たすプレイヤーが0名の場合、カード効果は不発となる。
* **効果**:
  * 対象全員に対し、個別に 50% の確率で **-1,000点** / 50% の確率で **-3,000点** のダメージを与える。
  * 対象全員の手札およびセットされている防御カードをすべて破棄する。
  * **選択不可状態の付与（貫通・上書きルール）**:
    * すでに対象が「選択不可状態」であっても効果・デバフを貫通して適用する。
    * 対象の選択不可状態を **「2ターン固定」へと上書き（更新）** する。
* **特記事項（例外処理）**:
  * 対象が「無敵状態」「ステロイド状態」の場合、ダメージ・カード破棄・選択不可状態付与のすべての効果が無効化される。

### ⑪ 時限爆弾 (TIME_BOMB)
* **分類**: 特殊
* **ドロー時挙動（即時強制発動）**:
  * 山札から引いた瞬間、手札に入らず自動で強制発動（「時限爆弾出現！」のカットイン演出）。
  * 自身の得点を **+1,000点** し、自分に **「時限爆弾状態（8ターン）」** を付与する（※防御カードのセット状態は維持される）。
* **「時限爆弾状態」の持続効果**:
  * ターン開始時効果: 保持者の自ターン開始時、保持者の得点を **+1,000点** する（※ドローしたターンは重複防止のため除外）。
  * 他バフによる消滅: 保持者が「無敵状態」または「ステロイド状態」になった場合、時限爆弾状態は即座に消滅する（爆発ペナルティなし）。
* **受け渡し（譲渡）処理**:
  * 自分のターン中、バッジをクリックして自分との得点差が **±3,000点以内** のプレイヤー（選択不可状態含む。無敵・ステロイドは除外）へ受け渡しを試行可能（試行後も自ターン継続）。
  * モーダル内では対象候補の得点差を **`+` / `-` / `±0` の符号付き** で直感的に表示。
  * 確率: **50%**。成功時は対象へ残りターン数を引き継いで移動。失敗時はそのターン中再試行不可。
  * **安全装置・自動ターン終了仕様**:
    * 未挑戦かつ譲渡対象が存在する場合、ターン終了ボタン押下時に確認モーダルが開き、「無視してターン終了」または「50%の確率で受け渡してターン終了」を選択可能。
    * 警告モーダル経由で受け渡しを実行した場合、**成否に関わらず自動でそのままターン終了処理（手札枚数チェック経由で次手番へ移行）を完了**させる（再度のターン終了ボタン押下を不要化）。
* **カウント0時の爆発ペナルティ**:
  * 8ターン経過しカウントが0になった瞬間、保持者に **-6,000点**、**手札・防御カード全破棄**、**選択不可状態（2ターン上書き）** を適用。

### ⑫ お守り小判 (OMAMORI_KOBAN)
* **分類**: 特殊
* **通常効果**: 使用すると自分の得点を **+3,000点** する。使用後破棄。
* **自動発動効果（ダイヤの剣カウンター）**:
  * 手札に所持している状態で自分が「ダイヤの剣」の対象になった時、手札のこのカードが自動で1枚消費される。
  * その「ダイヤの剣」の効果（ダメージ、手札・防御カード破棄、選択不可付与）を完全に無効化する。
  * 自動消費時にも、通常効果と同様に自分の得点が **+3,000点** される。

### ⑬ お守り小判セット (OMAMORI_KOBAN_SET)
* **分類**: 特殊 (使用回数: 3回)
* **通常効果（任意使用時）**:
  * 使用回数（1〜残り使用回数）を指定して手動使用可能。
  * 1回分につき自分の得点を **+2,000点** する（例: 2回分消費で +4,000点、3回分消費で +6,000点）。
  * 指定した消費回数分だけ残り回数が減少し、残り回数が 0 になった時点で手札から破棄される。
* **自動発動効果（ダイヤの剣カウンター）**:
  * 手札に所持している状態で自分が「ダイヤの剣」の対象になった時、自動で **1回分** が消費される。
  * その「ダイヤの剣」の効果（ダメージ、手札・防御カード破棄、選択不可付与）を完全に無効化する。
  * 自動消費時にも、自分の得点が **+2,000点** される。
  * 残り回数が 0 になると手札から破棄される（残り回数が残っていれば手札に残り、次回以降のダイヤの剣も防御可能）。

### ⑭ お守り大判 (OMAMORI_OBAN)
* **分類**: 特殊 (使用回数: 1回 使い切り)
* **排他制御ルール（同一ターン内制限・同種カード連続使用禁止）**:
  * 本カードを使用するターンは、手札からの他カード使用・防御セット、および**2枚目のお守り大判・ダークマターの使用が禁止**される。
  * **使用前判定**: 同ターン内にすでに手札から他カードを使用・セット済みの場合、使用不可（モーダル上でボタン非活性＆「※他のカードを使用したため発動できません」警告表示）。※前ターン以前にセットされた防御カードの攻撃転用・破棄は制限対象外。
  * **使用後判定**: 同ターン内に本カードを使用した場合、以降の手札カード使用・セット（2枚目の大判・ダークマター含む）がすべて禁止（モーダル上でボタン非活性＆「※「お守り大判」を使用したため発動できません」警告表示）。
  * ※「時限爆弾」の強制発動および受け渡し試行は本制限の影響を受けない。
* **通常効果（任意使用時）**:
  * モーダル上で **+3,000点 〜 +8,000点**（1,000点刻み、初期値: +8,000点）を選択し、選択した得点を獲得する。使用後破棄。
* **自動発動効果（ダイヤの剣カウンター）**:
  * 手札に所持している状態で自分が「ダイヤの剣」の対象になった時、手札のこのカードが自動で1枚消費される。
  * その「ダイヤの剣」の効果（ダメージ、手札・防御カード破棄、選択不可付与）を完全に無効化する。
  * 自動消費時にも、自分の得点が **+8,000点** される。

### ⑮ 大災害 (DISASTER)
* **分類**: 攻撃 (命中率: 100%)
* **対象**: 使用者以外の全員
* **効果**:
  * 手札およびセット中防御カードを全破棄。
  * 順位に応じたダメージ: 1位: -6,000点 / 2位: -4,000点 / 3位: -2,000点 / 4位: -1,000点。
  * ダメージを受けたプレイヤーに選択不可付与。
  * ※ステロイド状態の相手は大災害を防御するが、ステロイド状態が即時強制解除される。

### ⑯ 無敵アーマー (INVINCIBLE_ARMOR)
* **分類**: 特殊
* **公開範囲**: **初期は本人のみ表示**（攻撃を1度防ぐと全プレイヤーに公開）
* **効果**: 使用から合計4ターン経過まで「無敵状態」になる。解除時に+1,000点＆相手へペナルティ判定。

### ⑰ ダークマター (DARK_MATTER)
* **分類**: 特殊
* **排他制御ルール（同一ターン内制限・同種カード連続使用禁止）**:
  * 本カードを使用するターンは、手札からの他カード使用・防御セット、および**2枚目のダークマター・お守り大判の使用が禁止**される。
  * **使用前判定**: 同ターン内にすでに手札から他カードを使用・セット済みの場合、使用不可（モーダル上でボタン非活性＆「※他のカードを使用したため発動できません」警告表示）。※前ターン以前にセットされた防御カードの攻撃転用・破棄は制限対象外。
  * **使用後判定**: 同ターン内に本カードを使用した場合、以降の手札カード使用・セット（2枚目のダークマター・大判含む）がすべて禁止（モーダル上でボタン非活性＆「※「ダークマター」を使用したため発動できません」警告表示）。
  * ※「時限爆弾」の強制発動および受け渡し試行は本制限の影響を受けない。
* **UI表示**: 使用時のモーダル上に、5,000点獲得後にペナルティ対象（同点または追いつかれた相手）となるプレイヤー名を事前表示。
* **効果**:
  * 自身の得点を +5,000点。
  * 次の自分ターン開始時まで「無敵状態」になる。
  * 使用直後の同点・逆転相手（無敵・選択不可除く）へ50%で手札・防御全破棄＆-3,000点＆選択不可付与。

### ⑱ ステロイド (STEROID)
* **分類**: 特殊
* **公開範囲**: **初期は本人のみ表示**（攻撃を1度防ぐと全プレイヤーに公開）
* **効果**: 使用から合計4ターン経過まで「ステロイド状態」になる。解除時に+1,000点＆相手へペナルティ判定。

### ⑲ 煙幕 (SMOKE_SCREEN)
* **分類**: 攻撃
* **対象**: 自分以上の得点を持つ相手全員（該当者なしの場合は自身）
* **UI表示**: モーダル上に効果対象となる同点以上の相手プレイヤー名を表示（該当者なしの場合は自身への跳ね返り警告を表示）。
* **効果**:
  * 対象の得点を -1,000点 ＆ 「暗闇状態」付与。
  * 継続ターン数: 対象が1位なら2T、2位〜4位なら1T。
  * 自身への跳ね返り時、自身が無敵・ステロイド状態なら無効化。

---

## 4. UI・表示仕様

### ■ 攻撃カットインアニメーション演出システム（対峙・シームレス突進・射撃貫通・連撃ループ）
* **概要**:
  * 攻撃カード発動時、全画面カットインオーバーレイが起動し、攻撃者と対象者が左右で対峙する迫力あるバトル演出を全プレイヤーへ同期再生する。
* **画面構成**:
  * **左側（攻撃側）**: `[プレイヤー名 + アバター画像 + 攻撃カードイラスト]` の1ユニット。
  * **右側（対象側）**: `[プレイヤー名 + アバター画像]` の対象ユニット（対象が複数の場合は判定順・点差順に横一列で整列）。
  * **背景**: 集中線とスピードラインが高速で流れる暗転バトルフィールド。
* **アニメーション挙動と判定別演出**:
  1. **突進（木の盾・木の盾セット・青銅の盾・木の剣）**:
     * 攻撃側ユニットが左から右の対象の手前へ向かって高速突進。
  2. **射撃・弾道直進（ショットガン専用）**:
     * 攻撃ユニットは左側に留まり、マズルフラッシュとリコイル反動とともに高速の銃弾トレーサー（光線弾）を発射。
  3. **回避（ミス）**: 
     * 対象ユニットがサッと上方向へ回避（半透明化）し、青文字で「**回避！**」バッジが出現。突進・銃弾は止まらず次の対象へシームレスに直進。
  4. **命中（ヒット）**: 
     * 画面全体が白くフラッシュし、赤文字で「**命中！**」バッジが出現。対象ユニットが右斜め上へ回転しながら画面外へ吹き飛ぶ（演出完了／連撃時は離脱）。
  5. **防御（通常）**: 
     * 対象の手前にセット中の防御カードがポップアップ出現し、緑文字で「**防御！**」バッジが出現。攻撃ユニットが火花とともにノックバック。
  6. **防御（ショットガン貫通）**: 
     * 防御カードが出現するが、銃弾が直撃して紫文字で「**貫通！**」バッジが出現。防御カードごと打ち抜かれて画面外へ吹き飛ぶ。
  7. **無敵 / ステロイド**: 
     * 対象の周囲に金色のバリアオーラが出現し、橙文字で「**無敵！**」または「**ステロイド！**」バッジが出現。対象は微動だにせず突進・銃弾が弾き返される（連撃は即時中断）。
* **複数回攻撃（連撃ループ）の制御仕様**:
  * 「木の盾セット」等の複数回攻撃時は、指定回数分（最大3回）ラウンド制でアニメーションをループ再生。
  * 命中して吹き飛んだプレイヤーは盤面外に離脱した状態を維持し、次撃は残った対象へ初期位置から再度突進。
  * 「全員回避」や「防御カードでの無効化」でも指定回数は消費され、残存回数・対象があれば次撃へ継続。
  * 「無敵／ステロイドによる無効化」または「対象が全員消失」した瞬間に即座に連撃ループを終了。
* **完全同期制御**:
  * サーバー側で全判定結果シーケンスを確定後に演出イベント（`playAttackCutin`）を発火。
  * クライアント側のアニメーション終了タイミングに合わせて盤面同期（LP変動・ログ更新）が確定するため、ズレやコマ落ちが発生しない。

---

### ■ 数値の3桁カンマ区切り（カンマセパレート）表示の統一
* 画面上の持ち点、変動スコア、ツールチップ説明文、各種モーダル内の選択肢およびシステムメッセージに表示されるすべての数値は、3桁カンマ区切り（例: `25,000点`、`+3,000点`、`-5,000点`）で統一表示される。

### ■ ライフポイント（LP）風得点変動アニメーション演出
* **ステップⅠ（変動値ポップアップ）**: 得点変動時、対象プレイヤーの「持ち点: 〇〇点」テキストの**すぐ右隣（fit-content配置）**にプラス（緑色 `+3,000`）／マイナス（赤色 `-3,000`）の数値がポップアップ出現（表示時間: 1.15秒）。
* **ステップⅡ（吸い込み合流）**: ポップアップした変動値が持ち点数値へ向かってスッと吸い込まれるように移動・フェードアウト。
* **ステップⅢ（LPドラムロール変動）**: 吸い込みと同期して持ち点数値が高速カウントアップ／ダウン（750ms）し、完了時に確定パルス（フラッシュ）が発生。カットイン終了後に確実に発火するキュー待機構造を適用。

### ■ 画面右上「設定メニュー（⚙ボタン）」
* 画面右上の⚙ボタンから、プレイヤーごとの独立したユーザー設定モーダルを開く。
* **プレイヤーネーム変更機能**: 任意の表示名（最大10文字）を入力・変更可能。盤面・ダイアログ・中央ログエリア（過去ログ含む）へ即座に新名称が同期反映される。
* **プレイヤーアバター設定機能**: 
  * 盤面UI上に正方形枠（1:1）でアバター画像を表示。
  * **初期割り当て**: ゲーム開始時、全プレイヤーに共通の「デフォルトアバター（avatar_default）」が初期設定される。
  * **選択・変更ルール**: 設定メニューの選択一覧にはシステムプリセット画像のみが表示され、デフォルトアバターは含まれない（一度別アバターに変更した後はデフォルトに戻せない不可逆仕様）。
  * 今後システム側でアバター画像を随時追加・拡張可能な動的設計を適用。

### ■ 盤面UIレイアウト（ハイブリッド2段レイアウト）
* **左右枠（240px拡張）**: 上家・下家の表示枠を拡張し、視認性とゆとりを確保。
* **位置ラベルの廃止と自然折り返し**: `【上家】` などの位置ラベルテキストを廃止（自分自身のみ `(あなた)` と表示）。プレイヤーネームは `word-break: break-all;` により10文字以内であれば「…」と省略されず1〜2行ですべて表示。
* **バッジ専用行の新設**: プレイヤーネーム直下に独立したバッジ表示専用コンテナを配置。バッジが複数付与されても名前表示を一切圧迫せず、残ターン数の丸囲み数字も欠けずにクリアに描画。

### ■ デバッグUIの集約配置
* 画面左上にフローティング配置された「🛠 デバッグパネル」内に、各プレイヤーの得点操作・ドローボタン、ボーナススキップ設定、「他者情報公開/非公開」切り替えスイッチ、および「⚙ カード排出設定」モーダル呼び出しボタンを一括統合。

### ■ 他プレイヤー情報（手札・防御カード）の公開／非公開制御
* **公開モード（ON時 / デフォルト）**: 自分以外のプレイヤーの手札枚数およびセット中防御カードを常時表示。
* **非公開モード（OFF時）**: 
  * 相手プレイヤーの盤面UI上から「手札枚数」および「防御カード」の表示領域自体を完全に非表示化（コンポーネント非描画）。
  * ドローや防御セットに関する他者ログも非表示化。
  * **公開例外ルール**: 相手が防御カードで攻撃を無効化し、かつ耐久力（残り回数）が 1 以上残っている場合のみ、該当プレイヤーの盤面に「防御: 〇〇 (〇回)」表示領域が出現（可視化）する。

### ■ 手番進行インジケーター（手番完了ステータス）
* 各プレイヤー枠の右下エリアに、その巡目（ラウンド）の手番行動が完了したプレイヤーに対して **「済」バッジ** を表示。
* 巡目終了時に自動でリセットされ、誰が行動済みで誰が未行動なのかをひと目で視覚的に把握可能。

### ■ バフ/デバフバッジ
* 無敵アーマー・ダークマター・ステロイド・煙幕・時限爆弾はミニカードイラスト＋残ターン数バッジで表示。
* 無敵アーマー・ステロイドは、攻撃を防ぐまでは使用者本人にのみバッジが表示され、防いだ時点から全プレイヤーへ公開表示される。
* 「選択不可」は `[選択不可:X]` テキスト形式を維持。

### ■ ツールチップ表示位置の画面内自動補正
* 防御カード、ステータスバッジ、手札ホバー時のツールチップは、画面境界（ビューポート）を動的に検知して自動配置。
* 画面上端で見切れる場合は自動で下側に反転表示し、左右端ではみ出る場合も画面内に収まるようオフセット補正（長文テキストの見切れを完全防止）。

### ■ カードモーダルUI
* 攻撃対象や攻撃回数選択時、命中率（単体累積確率 / グループ順次判定確率 / ランダム選出確率）および暗闇状態による半減をリアルタイムに計算して表示。
* 「青銅の盾セット」のパターンA（単体攻撃）において、最寄りの同点プレイヤーが複数存在する場合は、指定攻撃回数 $k$ と対象人数 $m$ に応じた実質命中率（$\min(1.0, k/m)$）をリアルタイムに計算して表示。
* 各種範囲攻撃・特殊カード（地震・ダイヤの剣・ダークマター・煙幕）において、モーダル上に影響を受ける対象プレイヤー名をわかりやすく事前表示。
* 「お守り大判」「ダークマター」では、同一ターン内の他カード使用状況および同種カード使用状況に応じた排他警告・ボタン非活性制御をリアルタイムに適用（アラートではなくモーダル上で最初から非活性化）。
* 時限爆弾の受け渡しモーダルでは、対象プレイヤーとの得点差を `+` / `-` / `±0` 符号付きで明瞭に表示。ターン終了時警告モーダルからの譲渡時は自動でターン終了へ移行。
* モーダル内のカード説明文は改行コード（`\n`）やスタイル装飾を適切に反映して整形表示。セットカードの【残り回数】は手札・防御・プレビューすべてで動的に反映。
* 防御カードとしてセットできないカード（ショットガン、地震、お守り小判、お守り大判等）は、モーダル上に防御セットボタンを表示しない。

---

## 5. ゲーム内テキスト（文言）の統一と管理規約

### ■ 適用範囲
本規約は、ゲーム内でプレイヤーに対して表示されるすべてのテキスト表現および文言に適用される。
1. 各種カードの説明文（desc）および名称
2. ステータスバッジ（無敵・ステロイド・煙幕・時限爆弾・選択不可等）のツールチップ表示テキスト
3. 各種モーダル、ダイアログ上の見出し、説明文、ボタンテキスト（攻撃実行・防御セット・受け渡し・破棄等）
4. ゲームログエリアに流れるシステムメッセージおよびアクションログ
5. その他、ゲームプレイ中に画面上に表示されるすべてのテキスト群

### ■ 文言の策定・管理主体
* ゲーム内に表示されるすべての文言は、**仕様策定者（担当者）が直接記述・管理**を行う。

### ■ 変更・改変に関する禁止事項
1. **無断変更・アレンジの禁止**:
   * 明確な指示または事前の協議がない限り、プログラム実装時およびUIデザイン作成時において、テキスト文言・ニュアンス・言い回しを開発者側で勝手に変更・要約・アレンジすることを禁止する。
2. **システム制約時の事前確認義務**:
   * レイアウト崩れや文字数オーバー等のシステム上の制約により、どうしても文言の変更・短縮が必要となる場合は、開発者が独自に判断して変更せず、**必ず事前に仕様策定者へ確認・提案を行い、了承を得た上で修正**を行うこと。